{
  "//": "Metadata for the patch-package patches in this directory — one key per patched npm package. Consumed by the orphaned-patch guard (packages/dev-tools/patch-reconcile/check-patches.mjs, run as `npm run lint:patches`) and the renovate-patch-reconcile workflow. Every patches/<pkg>+<version>.patch MUST have an entry here with a matching patchedVersion. When a patch is removed, delete its entry. See patches/README.md.",
  "@zenfs/core": {
    "patchedVersion": "2.7.6",
    "upstream": "https://github.com/zen-fs/core/issues/318",
    "issue": "https://github.com/ai-ecoverse/slicc/issues/3101",
    "reason": "#2146: VCache.ref coalesced DIFFERENT paths onto one vnode when their inodes shared an ino (poisoned indexes carry many ino-0 entries) — the shared inode object cross-stamps size/mode between unrelated files. The patch refuses cross-path coalescing unless the inodes are a genuine hardlink: ino must be nonzero, format bits must match, AND nlink must be > 1 on both sides (#2034). A duplicated real ino with nlink 1 is a poisoned index, not a link — and because every vnode also owns its sparse data cache, two colliding paths open at once returned each other's bytes (8 parallel preview fetches all served the entry file; sequential reads evicted the vnode between calls and were correct). 2.7.6 (zen-fs/core#314) only emits a non-fatal error when nlink === paths.size on a new path; it still returns the shared vnode, so the refuse-to-coalesce hunk stays. 2.7.6 still only logs it. (The patch also used to carry an Index._alloc() hunk for zen-fs/core#312 — the Math.max spread of 2x entryCount arguments that threw RangeError('Maximum call stack size exceeded') on every createFile/mkdir once the OPFS index grew large. That fix shipped upstream in 2.6.5, which now reduces instead of spreading and memoizes the last id, so the hunk is gone; tests/fs/zenfs-alloc-overflow-upstream.test.ts pins the upstream behavior.) ALSO (zen-fs/core#318 / slicc#3101): core 2.7.6 acquires a whole-tree payload semaphore and dom 1.2.14 initializes it; SLICC configures the released limiter to 16, so the patch no longer maintains a second concurrency cap. The remaining crossCopy delta shares the first read/stat/readdir failure across the preload tree, checks it after a semaphore handoff before allocating or reading, awaits all descendants so active reads drain, and preserves the first error. Files larger than 1 MiB (SYNC_FS_MAX_FILE_BYTES) keep a name and size in the sync mirror but not a body: allocating every shard of a multi-gigabyte OPFS tree fails mount with NotReadableError and Array buffer allocation failed. touchSync grows that size only when metaOnly is absent, so the mirror is not zero-filled. A sync read of those paths throws. rename moves the marker with the inode, unlink and rmdir drop it, and write collapses the fictional size before storing the new bytes. Async writes and renames update the mirror through _sync directly, so the same bookkeeping runs from the async wrapper. ALSO (slicc#3441, upstream https://github.com/zen-fs/core/issues/322): the same 1 MiB cap now applies to async writes and touches after mount. Upstream copies every async write's bytes into the InMemory mirror and never lets go, so a multi-GB `hf download` kept each saved shard resident in the kernel worker until it ran out of memory (measured: worker ArrayBuffer backing store tracked OPFS usage 1:1). A write or touch that leaves a file above the cap drops any mirrored body and records name and size only (metaOnly); an offset-0 rewrite that ends at or below the cap falls through to the old path and restores real bytes. Async reads still hit the backend. Browser repro: packages/webapp/tests/e2e/zenfs-preload/.",
    "removeWhen": "Keep the VCache hunk until @zenfs/core refuses to share a vnode across paths for non-hardlink inodes — 2.7.6's zen-fs/core#314 only logs the mismatch. (The patch also used to rewrite `globalThis.__zenfs__` into an own-property copy so a probe could enumerate it; upstream declined that in https://github.com/zen-fs/core/issues/310 — the global is deliberately minimal — so that hunk is gone and consumers must probe by existence plus the `_version` export instead.) Remove the preload error-path hunk once Async.crossCopy shares the first traversal failure, starts no queued backend work after failure, and waits for issued work before ready() rejects. The concurrency limiter itself is already upstream in core 2.7.6 + dom 1.2.14 and must not be reintroduced here. Remove the async-write mirror cap (_mirrorMetaOnly) once Async stops mirroring bodies above a size limit into its sync cache (reported upstream as https://github.com/zen-fs/core/issues/322), or once SLICC runs with the async cache disabled.",
    "verify": "npm run test -w @slicc/webapp -- zenfs-ino-collision-patch virtual-fs-concurrent-read zenfs-alloc-overflow-upstream zenfs-preload-concurrency zenfs-sync-mirror-write-cap"
  },
  "@zenfs/dom": {
    "patchedVersion": "1.2.14",
    "upstream": "https://github.com/zen-fs/dom/issues/46",
    "issue": null,
    "reason": "#2146 leftover: 1.2.14 (zen-fs/dom#43) allocates unique ino/data pairs in WebAccessFS.stat's ENOENT recovery and _loadMetadata's reality branch, so the minting hunks are gone; tests/fs/zenfs-ino-collision-patch.test.ts pins the upstream _alloc(). Those allocations still omit nlink, and ZenFS's Inode constructor warns on every ino!=0/nlink==0 inode while kerium retains every log entry — leaving nlink unset flooded the in-memory log on every inode materialization until its backing Set hit V8's 2^24 cap and every FS op threw 'Set maximum size exceeded' (2026-08-18 VFS-offline incident). The remaining hunk sets nlink: 1 at both sites. ALSO (#2157): 1.2.14 (zen-fs/dom#45) now materializes zero-byte files through WebAccessFS._create and IndexFS.createFile rolls its index insert back if creation fails, so the redundant createFile override is gone. The patch still makes remove tolerate an already-absent OPFS entry — IndexFS has already validated existence against the authoritative index, so an entry it knows but OPFS does not (a legacy file or a sidecar entry whose bytes vanished) would otherwise be undeletable forever. (The patch also used to carry a keepExistingData hunk in write; that fix shipped upstream in 1.2.11 via WebAccessFS.touch.) ALSO (zen-fs/dom#46): a concurrent overwrite invalidates the File returned by getFile(), so arrayBuffer() throws NotReadableError even for a six-byte file. getFile() itself throws the same NotReadableError once a previous snapshot is still held, which a retry wrapped only around arrayBuffer() never sees. Reacquire via getFile() for at most three attempts, pausing briefly between them; other errors and persistent failures still propagate. In a worker, read() prefers createSyncAccessHandle and fills the caller's buffer directly, so an ONNX shard is not snapshotted via getFile(); a locked handle or a main-thread caller falls through to that retry. The real-Chromium repro controls the overwrite interleaving without injecting an exception: packages/webapp/tests/e2e/zenfs-opfs-read-race/. ALSO (#3441, upstream https://github.com/zen-fs/dom/issues/47): write() opened createWritable({ keepExistingData: true }) for every write, and Chromium copies the whole file into a swap file first, so a streamed multi-GB hf download appended in 8 MiB pieces cost O(n^2) disk I/O and briefly doubled OPFS quota. In a worker, write() now writes in place through createSyncAccessHandle; a locked handle or a main-thread caller falls through to createWritable.",
    "removeWhen": "Split by hunk. The nlink: 1 hunks can drop once WebAccess sets nlink at both minting sites (1.2.14 still omits it; Inode still warns on ino!=0/nlink==0). The zero-byte create side is upstream in 1.2.14 via WebAccessFS._create. The remove hunk can drop once WebAccessFS.remove tolerates an OPFS entry the index knows but the store does not (1.2.14 still rethrows ENOENT). Remove the snapshot-read hunk once zen-fs/dom#46 ships bounded NotReadableError retries with freshly acquired File snapshots. Remove the sync-access-handle write hunk once WebAccessFS.write stops copying the file on every write (no keepExistingData swap copy for in-place/append writes; reported upstream as https://github.com/zen-fs/dom/issues/47).",
    "verify": "npm run test -w @slicc/webapp -- zenfs-dom-truncate-upstream zero-byte-file-delete zenfs-ino-collision-patch zenfs-opfs-read-retry"
  },
  "isomorphic-git": {
    "patchedVersion": "1.42.2",
    "upstream": "https://github.com/isomorphic-git/isomorphic-git",
    "issue": "https://github.com/ai-ecoverse/slicc/issues/2710",
    "reason": "#2735: `readObjectPacked`'s packfile integrity check is guarded by a `p._checksumVerified` boolean that is only set AFTER its `await`, so every reader that arrives while the first SHA-1 is in flight starts its own hash of the WHOLE pack — `_walk` fans object reads out with `Promise.all`, so the 92 MB slicc pack was hashed 8-9 times per command (5,195 ms in `digest` on a hostfs mount vs 603 ms for one hash), each duplicate also copying the input for WebCrypto. The patch extracts the check into `verifyPackfileIntegrity()` and memoizes it as a promise on the cached pack index (`p._verification`), so N concurrent readers verify exactly once. The memo records whether it INCLUDED the deep hash (`p._deepVerified`), so a caller that turns verification back on mid-shell is not served the promise of the run that skipped it. #2710: the deep (full payload) SHA-1 has no upstream switch, and it is not something canonical git does on an object read (it verifies packs on `fsck` / `index-pack`); the patch skips it when the caller's cache object carries `skipDeepPackVerification`, which SLICC sets by default for its VFS/hostfs-backed repos (`packages/webapp/src/git/git-cache.ts`, `$SLICC_GIT_VERIFY_PACKS=1` opts back in). The O(1) trailer check always runs. The patch also stamps a monotonic `_lastUsedAt` on each pack index it serves a read from, which is what lets SLICC's long-lived cache evict the least recently used pack BUFFERS instead of holding every pack it ever touched. Only `index.js` (the ESM entry) is patched: the browser build and vitest both alias `isomorphic-git` to it on purpose because the CJS entry imports Node crypto (tests/git/isomorphic-git-esm.test.ts).",
    "removeWhen": "Remove the memoization hunk once isomorphic-git verifies a packfile once per pack rather than once per concurrent reader, and the skip hunk once it exposes an option to skip the deep payload verification on read.",
    "verify": "npm run test -w @slicc/webapp -- git-pack-cache"
  },
  "kerium": {
    "patchedVersion": "1.5.0",
    "upstream": "https://github.com/zen-fs/kerium",
    "issue": null,
    "reason": "kerium's log() retains EVERY entry in a module-global List (a plain Set underneath) with no cap or rotation — even entries below the output threshold that are never printed (default minLevel is ALERT). A long-lived kernel worker under log pressure (e.g. ZenFS's per-inode nlink warning) eventually hits V8's 2^24-element Set cap, after which entries.add throws RangeError('Set maximum size exceeded') into every logging caller — every ZenFS op failed and the VFS went offline (2026-08-18 incident). The patch evicts the oldest entry beyond a 10,000-entry ring so the backlog stays bounded.",
    "removeWhen": "Remove once kerium bounds its log backlog upstream (ring buffer or opt-in retention).",
    "verify": "npm run test -w @slicc/webapp -- kerium-log-cap-patch"
  },
  "just-bash": {
    "patchedVersion": "3.4.2",
    "upstream": "https://github.com/vercel-labs/just-bash — slicc-filed issues for the interop hunks: https://github.com/vercel-labs/just-bash/issues/456 (positionals past $9), https://github.com/vercel-labs/just-bash/issues/457 (command-word splitting), https://github.com/vercel-labs/just-bash/issues/458 (shebang dispatch), https://github.com/vercel-labs/just-bash/issues/459 (ls -l modes), https://github.com/vercel-labs/just-bash/issues/460 (expr BRE), https://github.com/vercel-labs/just-bash/issues/461 (case [!...] negation), https://github.com/vercel-labs/just-bash/issues/463 (pipe stdin after exec <), https://github.com/vercel-labs/just-bash/issues/464 (ls -i), https://github.com/vercel-labs/just-bash/issues/465 (ls -t / -L)",
    "issue": null,
    "reason": "curl formats the response for stdout (`N(...)` → latin1 decode of the WHOLE body via `Buffer.from(body).toString('binary')`) BEFORE checking `-o`/`-O`, then discards the string. In the browser the `buffer` polyfill's latin1Slice builds that string one `+=` per byte, so every downloaded byte costs a ~32-byte V8 cons-string node: a 250 MB `curl -o` needed ~8 GB of V8 heap and crashed the leader tab (renderer OOM → Inspector.targetCrashed). The patch skips the stdout formatting when the body is going to a file (unless `-v`, which still prints it), so `curl -o` costs the body bytes, not a string copy of them. Multipart file fields use the optional readFileBytes API (binary read fallback); headers and inline fields are encoded as UTF-8 bytes before composing the latin1 body, preserving binary uploads alongside Unicode text. The chmod patch preserves actual filesystem errors (including ENOSYS, EACCES, and EIO) instead of reporting every failure as ENOENT, and recognizes -- before literal --help operands. This permits upstream symbolic/recursive mode handling and traversal limits to replace the earlier unsupported-mode overlay. ALSO (#3285): `execInScope` with `{ env, replaceEnv: true }` copied the parent `exportedVars` set onto the child state, so `sh -c`/`bash -c` (which inherit `exportedEnv`, not the full env map) dropped caller env while a directly-executed binary still saw it. The hunk rebuilds `exportedVars` from the replaced map (and adds merged `env` keys). ALSO: a script run by path (how SLICC runs every executable file) and `source` set only $1..$9, while \"$@\"/$# read $1..$N, so the 10th argument onward expanded to empty strings — an emcc launcher forwarding an 18-argument clang command line lost its input file. The hunks set every positional, clear stale ones above the new count, and make `source` save/restore all of them. ALSO: the command word was expanded as one string (no word splitting), so `$CC -c x.c` with CC=\"emcc -O2\" (or zlib configure's `( $* )`) looked for a command literally named \"emcc -O2\". The hunk expands the name with the splitting expander, runs the first field and prepends the rest to the arguments. ALSO: `ls -l` printed a hardcoded -rw-r--r-- / drwxr-xr-x instead of the stat mode, so an executable ./configure looked non-executable; the hunks render the real permission bits. ALSO: a script run by path always ran as bash with its shebang dropped, so a program built by `emcc -o example` (#!/usr/bin/env node) failed to parse. A non-shell shebang now runs `<shebang line> \"$0\" \"$@\"`; sh/bash/dash/ash/ksh/zsh shebangs and `bash file` keep the bash path. ALSO: `expr STRING : REGEX` handed the pattern to RegExp as-is, but POSIX expr takes a basic regex where \\( \\) group (and bare parens are literal), so every autoconf configure died on `expr \"x$ac_option\" : 'x\\([^=]*\\)='` (\"invalid variable name: 0\"). The hunk converts BRE to JS regex syntax for `:` and `match`. ALSO: `case` / `[[ == ]]` patterns copied a bracket expression into the RegExp verbatim, so `[!...]` meant \"! or ...\" (JS negates with ^) and `[[:alpha:]]` never matched; autoconf's `case $ac_envvar in *[!_$as_cr_alnum]*)` then rejected every VAR=value (\"invalid variable name\"). The hunk translates POSIX bracket expressions (leading ! or ^, a leading ], named classes, escapes). ALSO: after a persistent `exec </dev/null`, redirection prep seeded every command's stdin from fd 0, even a pipeline stage fed by a pipe, so `printf hi | cat` printed nothing; autoconf runs `exec 7<&0 </dev/null` early, which emptied every later `$(… | …)` (e.g. `--disable-shared` became \"unrecognized\"). The hunk marks stages after the first as piped and skips the fd-0 seed for them. ALSO: `ls -i` was rejected, so autoconf's `ls -di .` working-directory check failed (\"working directory cannot be determined\"); the hunk adds -i/--inode for the -d form (stat().ino, else a stable hash of the resolved path). ALSO: `ls -t` never sorted by mtime and `ls -L` was rejected, so automake's sanity check (`ls -Lt configure conftest.file`, expecting the new file first) failed with \"newly created file is older than distributed files\"; the hunk accepts -L and sorts operands and directory entries by mtime (newest first, name as tiebreak, -r honored).",
    "removeWhen": "Remove once just-bash's curl only formats the body for stdout when it will be printed (or streams `-o` output to the fs). The multipart form builder must also read raw file bytes and encode text fields separately. Remove the chmod hunks once upstream preserves backend errors and treats --help after -- as an operand. Remove the exportedVars hunk once `exec({ env, replaceEnv: true })` marks those keys exported so `sh -c`/`bash -c` inherit them. Remove the positional hunks once script-by-path and `source` set more than nine positionals (vercel-labs/just-bash#456). Remove the command-name hunk once an unquoted expansion in command position is word-split (vercel-labs/just-bash#457). Remove the ls hunks once `ls -l` renders stat().mode (vercel-labs/just-bash#459). Remove the shebang hunk once scripts run by path honour a non-shell interpreter line (vercel-labs/just-bash#458). Remove the expr hunk once expr treats its pattern as a POSIX basic regex (vercel-labs/just-bash#460). Remove the bracket hunk once case / [[ == ]] patterns support [!...] negation (vercel-labs/just-bash#461). Remove the pipe-stdin hunk once a pipe feeds its stage after `exec <` (vercel-labs/just-bash#463). Remove the ls -i hunk once ls supports -i (vercel-labs/just-bash#464). Remove the ls -t / -L hunk once ls sorts by mtime and accepts -L (vercel-labs/just-bash#465).",
    "verify": "npm run test -w @slicc/webapp -- just-bash-curl-output-patch curl-request-body-encoding just-bash-chmod-patch chmod-exec-bit just-bash-replace-env-exported just-bash-positional-args-patch just-bash-command-name-split-patch just-bash-ls-mode-patch just-bash-shebang-patch just-bash-expr-bre-patch just-bash-case-bracket-patch just-bash-exec-stdin-pipe-patch just-bash-ls-inode-patch just-bash-ls-time-patch"
  }
}
