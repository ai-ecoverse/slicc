diff --git a/node_modules/isomorphic-git/index.cjs b/node_modules/isomorphic-git/index.cjs
index 65481d7..137a747 100644
--- a/node_modules/isomorphic-git/index.cjs
+++ b/node_modules/isomorphic-git/index.cjs
@@ -3626,37 +3626,37 @@ async function readObjectPacked({
       }
 
       // === Packfile Integrity Verification ===
-      // Performance optimization: use _checksumVerified flag to verify only once per packfile
-      if (!p._checksumVerified) {
-        const expectedShaFromIndex = p.packfileSha;
-
-        // 1. Fast Check: Verify packfile trailer matches index record
-        // Use subarray instead of slice to avoid memory copy (zero-copy for large packfiles)
-        const packTrailer = pack.subarray(-20);
-        const packTrailerSha = Array.from(packTrailer)
-          .map(b => b.toString(16).padStart(2, '0'))
-          .join('');
-        if (packTrailerSha !== expectedShaFromIndex) {
-          throw new InternalError(
-            `Packfile trailer mismatch: expected ${expectedShaFromIndex}, got ${packTrailerSha}. The packfile may be corrupted.`
-          )
-        }
-
-        // 2. Deep Integrity Check: Calculate actual SHA-1 of packfile payload.
-        // The Node package build swaps in a chunked implementation for large packs.
-        const actualPayloadSha = await shasumRange(pack, {
-          start: 0,
-          end: pack.length - 20,
-        });
-        if (actualPayloadSha !== expectedShaFromIndex) {
-          throw new InternalError(
-            `Packfile payload corrupted: calculated ${actualPayloadSha} but expected ${expectedShaFromIndex}. The packfile may have been tampered with.`
-          )
-        }
+      // Memoize the in-flight verification so concurrent readers share one SHA-1 pass.
+      if (!p._checksumVerification) {
+        p._checksumVerification = (async () => {
+          const expectedShaFromIndex = p.packfileSha;
+
+          // 1. Fast Check: Verify packfile trailer matches index record
+          // Use subarray instead of slice to avoid memory copy (zero-copy for large packfiles)
+          const packTrailer = pack.subarray(-20);
+          const packTrailerSha = Array.from(packTrailer)
+            .map(b => b.toString(16).padStart(2, '0'))
+            .join('');
+          if (packTrailerSha !== expectedShaFromIndex) {
+            throw new InternalError(
+              `Packfile trailer mismatch: expected ${expectedShaFromIndex}, got ${packTrailerSha}. The packfile may be corrupted.`
+            )
+          }
 
-        // Mark as verified to prevent performance regression on subsequent reads
-        p._checksumVerified = true;
+          // 2. Deep Integrity Check: Calculate actual SHA-1 of packfile payload.
+          // The Node package build swaps in a chunked implementation for large packs.
+          const actualPayloadSha = await shasumRange(pack, {
+            start: 0,
+            end: pack.length - 20,
+          });
+          if (actualPayloadSha !== expectedShaFromIndex) {
+            throw new InternalError(
+              `Packfile payload corrupted: calculated ${actualPayloadSha} but expected ${expectedShaFromIndex}. The packfile may have been tampered with.`
+            )
+          }
+        })();
       }
+      await p._checksumVerification;
 
       const result = await p.read({ oid, getExternalRefDelta });
       result.format = 'content';
diff --git a/node_modules/isomorphic-git/index.js b/node_modules/isomorphic-git/index.js
index bbdc7f0..f840821 100644
--- a/node_modules/isomorphic-git/index.js
+++ b/node_modules/isomorphic-git/index.js
@@ -3613,37 +3613,37 @@ async function readObjectPacked({
       }
 
       // === Packfile Integrity Verification ===
-      // Performance optimization: use _checksumVerified flag to verify only once per packfile
-      if (!p._checksumVerified) {
-        const expectedShaFromIndex = p.packfileSha;
-
-        // 1. Fast Check: Verify packfile trailer matches index record
-        // Use subarray instead of slice to avoid memory copy (zero-copy for large packfiles)
-        const packTrailer = pack.subarray(-20);
-        const packTrailerSha = Array.from(packTrailer)
-          .map(b => b.toString(16).padStart(2, '0'))
-          .join('');
-        if (packTrailerSha !== expectedShaFromIndex) {
-          throw new InternalError(
-            `Packfile trailer mismatch: expected ${expectedShaFromIndex}, got ${packTrailerSha}. The packfile may be corrupted.`
-          )
-        }
-
-        // 2. Deep Integrity Check: Calculate actual SHA-1 of packfile payload.
-        // The Node package build swaps in a chunked implementation for large packs.
-        const actualPayloadSha = await shasumRange(pack, {
-          start: 0,
-          end: pack.length - 20,
-        });
-        if (actualPayloadSha !== expectedShaFromIndex) {
-          throw new InternalError(
-            `Packfile payload corrupted: calculated ${actualPayloadSha} but expected ${expectedShaFromIndex}. The packfile may have been tampered with.`
-          )
-        }
+      // Memoize the in-flight verification so concurrent readers share one SHA-1 pass.
+      if (!p._checksumVerification) {
+        p._checksumVerification = (async () => {
+          const expectedShaFromIndex = p.packfileSha;
+
+          // 1. Fast Check: Verify packfile trailer matches index record
+          // Use subarray instead of slice to avoid memory copy (zero-copy for large packfiles)
+          const packTrailer = pack.subarray(-20);
+          const packTrailerSha = Array.from(packTrailer)
+            .map(b => b.toString(16).padStart(2, '0'))
+            .join('');
+          if (packTrailerSha !== expectedShaFromIndex) {
+            throw new InternalError(
+              `Packfile trailer mismatch: expected ${expectedShaFromIndex}, got ${packTrailerSha}. The packfile may be corrupted.`
+            )
+          }
 
-        // Mark as verified to prevent performance regression on subsequent reads
-        p._checksumVerified = true;
+          // 2. Deep Integrity Check: Calculate actual SHA-1 of packfile payload.
+          // The Node package build swaps in a chunked implementation for large packs.
+          const actualPayloadSha = await shasumRange(pack, {
+            start: 0,
+            end: pack.length - 20,
+          });
+          if (actualPayloadSha !== expectedShaFromIndex) {
+            throw new InternalError(
+              `Packfile payload corrupted: calculated ${actualPayloadSha} but expected ${expectedShaFromIndex}. The packfile may have been tampered with.`
+            )
+          }
+        })();
       }
+      await p._checksumVerification;
 
       const result = await p.read({ oid, getExternalRefDelta });
       result.format = 'content';
diff --git a/node_modules/isomorphic-git/managers/index.cjs b/node_modules/isomorphic-git/managers/index.cjs
index 949a55c..3583a29 100644
--- a/node_modules/isomorphic-git/managers/index.cjs
+++ b/node_modules/isomorphic-git/managers/index.cjs
@@ -4426,37 +4426,37 @@ async function readObjectPacked({
       }
 
       // === Packfile Integrity Verification ===
-      // Performance optimization: use _checksumVerified flag to verify only once per packfile
-      if (!p._checksumVerified) {
-        const expectedShaFromIndex = p.packfileSha;
-
-        // 1. Fast Check: Verify packfile trailer matches index record
-        // Use subarray instead of slice to avoid memory copy (zero-copy for large packfiles)
-        const packTrailer = pack.subarray(-20);
-        const packTrailerSha = Array.from(packTrailer)
-          .map(b => b.toString(16).padStart(2, '0'))
-          .join('');
-        if (packTrailerSha !== expectedShaFromIndex) {
-          throw new InternalError(
-            `Packfile trailer mismatch: expected ${expectedShaFromIndex}, got ${packTrailerSha}. The packfile may be corrupted.`
-          )
-        }
-
-        // 2. Deep Integrity Check: Calculate actual SHA-1 of packfile payload.
-        // The Node package build swaps in a chunked implementation for large packs.
-        const actualPayloadSha = await shasumRange(pack, {
-          start: 0,
-          end: pack.length - 20,
-        });
-        if (actualPayloadSha !== expectedShaFromIndex) {
-          throw new InternalError(
-            `Packfile payload corrupted: calculated ${actualPayloadSha} but expected ${expectedShaFromIndex}. The packfile may have been tampered with.`
-          )
-        }
+      // Memoize the in-flight verification so concurrent readers share one SHA-1 pass.
+      if (!p._checksumVerification) {
+        p._checksumVerification = (async () => {
+          const expectedShaFromIndex = p.packfileSha;
+
+          // 1. Fast Check: Verify packfile trailer matches index record
+          // Use subarray instead of slice to avoid memory copy (zero-copy for large packfiles)
+          const packTrailer = pack.subarray(-20);
+          const packTrailerSha = Array.from(packTrailer)
+            .map(b => b.toString(16).padStart(2, '0'))
+            .join('');
+          if (packTrailerSha !== expectedShaFromIndex) {
+            throw new InternalError(
+              `Packfile trailer mismatch: expected ${expectedShaFromIndex}, got ${packTrailerSha}. The packfile may be corrupted.`
+            )
+          }
 
-        // Mark as verified to prevent performance regression on subsequent reads
-        p._checksumVerified = true;
+          // 2. Deep Integrity Check: Calculate actual SHA-1 of packfile payload.
+          // The Node package build swaps in a chunked implementation for large packs.
+          const actualPayloadSha = await shasumRange(pack, {
+            start: 0,
+            end: pack.length - 20,
+          });
+          if (actualPayloadSha !== expectedShaFromIndex) {
+            throw new InternalError(
+              `Packfile payload corrupted: calculated ${actualPayloadSha} but expected ${expectedShaFromIndex}. The packfile may have been tampered with.`
+            )
+          }
+        })();
       }
+      await p._checksumVerification;
 
       const result = await p.read({ oid, getExternalRefDelta });
       result.format = 'content';
diff --git a/node_modules/isomorphic-git/managers/index.js b/node_modules/isomorphic-git/managers/index.js
index 132fe09..af7242e 100644
--- a/node_modules/isomorphic-git/managers/index.js
+++ b/node_modules/isomorphic-git/managers/index.js
@@ -4413,37 +4413,37 @@ async function readObjectPacked({
       }
 
       // === Packfile Integrity Verification ===
-      // Performance optimization: use _checksumVerified flag to verify only once per packfile
-      if (!p._checksumVerified) {
-        const expectedShaFromIndex = p.packfileSha;
-
-        // 1. Fast Check: Verify packfile trailer matches index record
-        // Use subarray instead of slice to avoid memory copy (zero-copy for large packfiles)
-        const packTrailer = pack.subarray(-20);
-        const packTrailerSha = Array.from(packTrailer)
-          .map(b => b.toString(16).padStart(2, '0'))
-          .join('');
-        if (packTrailerSha !== expectedShaFromIndex) {
-          throw new InternalError(
-            `Packfile trailer mismatch: expected ${expectedShaFromIndex}, got ${packTrailerSha}. The packfile may be corrupted.`
-          )
-        }
-
-        // 2. Deep Integrity Check: Calculate actual SHA-1 of packfile payload.
-        // The Node package build swaps in a chunked implementation for large packs.
-        const actualPayloadSha = await shasumRange(pack, {
-          start: 0,
-          end: pack.length - 20,
-        });
-        if (actualPayloadSha !== expectedShaFromIndex) {
-          throw new InternalError(
-            `Packfile payload corrupted: calculated ${actualPayloadSha} but expected ${expectedShaFromIndex}. The packfile may have been tampered with.`
-          )
-        }
+      // Memoize the in-flight verification so concurrent readers share one SHA-1 pass.
+      if (!p._checksumVerification) {
+        p._checksumVerification = (async () => {
+          const expectedShaFromIndex = p.packfileSha;
+
+          // 1. Fast Check: Verify packfile trailer matches index record
+          // Use subarray instead of slice to avoid memory copy (zero-copy for large packfiles)
+          const packTrailer = pack.subarray(-20);
+          const packTrailerSha = Array.from(packTrailer)
+            .map(b => b.toString(16).padStart(2, '0'))
+            .join('');
+          if (packTrailerSha !== expectedShaFromIndex) {
+            throw new InternalError(
+              `Packfile trailer mismatch: expected ${expectedShaFromIndex}, got ${packTrailerSha}. The packfile may be corrupted.`
+            )
+          }
 
-        // Mark as verified to prevent performance regression on subsequent reads
-        p._checksumVerified = true;
+          // 2. Deep Integrity Check: Calculate actual SHA-1 of packfile payload.
+          // The Node package build swaps in a chunked implementation for large packs.
+          const actualPayloadSha = await shasumRange(pack, {
+            start: 0,
+            end: pack.length - 20,
+          });
+          if (actualPayloadSha !== expectedShaFromIndex) {
+            throw new InternalError(
+              `Packfile payload corrupted: calculated ${actualPayloadSha} but expected ${expectedShaFromIndex}. The packfile may have been tampered with.`
+            )
+          }
+        })();
       }
+      await p._checksumVerification;
 
       const result = await p.read({ oid, getExternalRefDelta });
       result.format = 'content';
