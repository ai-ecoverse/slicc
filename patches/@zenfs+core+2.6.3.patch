diff --git a/node_modules/@zenfs/core/dist/internal/file_index.js b/node_modules/@zenfs/core/dist/internal/file_index.js
index 399050b..3054275 100644
--- a/node_modules/@zenfs/core/dist/internal/file_index.js
+++ b/node_modules/@zenfs/core/dist/internal/file_index.js
@@ -79,7 +79,24 @@ export class Index extends Map {
      * @internal
      */
     _alloc() {
-        return Math.max(...[...this.values()].flatMap(i => [i.ino, i.data])) + 1;
+        // PATCH(zen-fs/core#312): the original spread `2 * entryCount`
+        // arguments into `Math.max`, so every createFile/mkdir threw
+        // `RangeError: Maximum call stack size exceeded` once the index grew
+        // past the host's spread-argument ceiling — and, because that ceiling
+        // is really "arguments vs. remaining stack", intermittently well
+        // before it, depending on how deep the async FS chain already was.
+        // The reduction has no argument limit and allocates nothing.
+        // Only difference: an EMPTY index yields 1 here and -Infinity
+        // upstream. `IndexFS.create` requires the parent entry to exist,
+        // so it can never reach `_alloc` on an empty index.
+        let max = 0;
+        for (const i of this.values()) {
+            if (i.ino > max)
+                max = i.ino;
+            if (i.data > max)
+                max = i.data;
+        }
+        return max + 1;
     }
     /**
      * Gets a list of entries for each directory in the index.
diff --git a/node_modules/@zenfs/core/dist/vfs/vcache.js b/node_modules/@zenfs/core/dist/vfs/vcache.js
index e978bba..0e00a9e 100644
--- a/node_modules/@zenfs/core/dist/vfs/vcache.js
+++ b/node_modules/@zenfs/core/dist/vfs/vcache.js
@@ -76,6 +76,27 @@ export class VCache {
      */
     ref(path, inode) {
         let node = this.byIno.get(inode.ino);
+        // PATCH(#2146, #2034): never coalesce DIFFERENT paths onto one vnode
+        // unless they are a genuine hardlink. Hardlinks share a real ino, a
+        // mode, AND carry nlink > 1; anything else with a matching ino is a
+        // poisoned index (ino-0 floods and duplicated ids both observed in
+        // the field). A shared vnode shares its inode object (stamping one
+        // file's size/mode onto the other on sync) and its data cache —
+        // which is why concurrent reads of two colliding paths returned the
+        // same bytes (#2034): the second open found the first file's vnode
+        // and its cached regions, while sequential reads evicted the vnode
+        // between calls and never collided.
+        if (node && !node.paths.has(path)
+            && (inode.ino === 0
+                || inode.nlink <= 1
+                || node.inode.nlink <= 1
+                || ((node.inode.mode ^ inode.mode) & 0o170000) !== 0)) {
+            // The byIno slot may have been claimed by ANOTHER colliding path;
+            // a re-ref of an already-known path must still find its own vnode.
+            node = this.byPath.get(path);
+            if (node && ((node.inode.mode ^ inode.mode) & 0o170000) !== 0)
+                node = undefined;
+        }
         if (!node) {
             node = new VNode(this.fs, path, inode);
             this.byIno.set(inode.ino, node);
