# Native computer capture evidence (#3248)

- `driving-chip.png` — isolated `:5715` harness lightbox on `jsh:fake` after a click; driving chip and clock/marker frame.
- `lightbox-driving.mp4` — overlay → lightbox drive-mode screencast (ffmpeg H.264 from the Playwright webm).
- `ios-dock-browser-computers.png` — iOS simulator follower joined to that isolated leader via a real Join URL; live `jsh:fake` card (not the cyan UITestHooks fixture).
- `ios-dock-browser-computer-live.png` — full-screen live view of the same fake computer (clock `04:20:48`, frame `#42`, Home/Back/Menu). Home was pressed after this still; the leader had already been torn down so the marker did not move.
