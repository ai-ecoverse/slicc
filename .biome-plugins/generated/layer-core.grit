or {
    `import $_ from $source`,
    `export $_ from $source`,
    `import($source)`,
    `require($source)`
} where {
    $source <: r".*(?:\.\./)+(?:scoops|ui)/.*",
    register_diagnostic(
        span = $source,
        message = "core/ must not import higher layers: scoops/, ui/.",
        severity = "error"
    )
}
