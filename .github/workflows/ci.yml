name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [automation/planning-artifact-cleanup]
  merge_group:
    branches: [main]

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

# Prefer IPv4 when resolving registry.npmjs.org. Actions runners routinely
# hand back an unreachable IPv6 route, and `npm ci` then dies with a hard
# `ENETUNREACH connect … :443` that npm's fetch-retries cannot catch (the
# failure is at the socket layer, before any HTTP retry engages). This was
# ~79% of observed CI flakes.
env:
  NODE_OPTIONS: --dns-result-order=ipv4first

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      webapp: ${{ steps.filter.outputs.webapp }}
      vfs-root: ${{ steps.filter.outputs.vfs-root }}
      assets: ${{ steps.filter.outputs.assets }}
      cloud-core: ${{ steps.filter.outputs.cloud-core }}
      node-server: ${{ steps.filter.outputs.node-server }}
      shared: ${{ steps.filter.outputs.shared }}
      webcomponents: ${{ steps.filter.outputs.webcomponents }}
      spoon: ${{ steps.filter.outputs.spoon }}
      extension: ${{ steps.filter.outputs.extension }}
      cloudflare-worker: ${{ steps.filter.outputs.cloudflare-worker }}
      cherry: ${{ steps.filter.outputs.cherry }}
      dev-tools: ${{ steps.filter.outputs.dev-tools }}
      capability-contract: ${{ steps.filter.outputs.capability-contract }}
      swift-server: ${{ steps.filter.outputs.swift-server }}
      swift-optel: ${{ steps.filter.outputs.swift-optel }}
      swift-traysession: ${{ steps.filter.outputs.swift-traysession }}
      swift-trayfollower: ${{ steps.filter.outputs.swift-trayfollower }}
      swift-traykit: ${{ steps.filter.outputs.swift-traykit }}
      swift-widgetkit: ${{ steps.filter.outputs.swift-widgetkit }}
      swift-launcher: ${{ steps.filter.outputs.swift-launcher }}
      ios-app: ${{ steps.filter.outputs.ios-app }}
      go-optel: ${{ steps.filter.outputs.go-optel }}
      slicc-cli: ${{ steps.filter.outputs.slicc-cli }}
      swift-config: ${{ steps.filter.outputs.swift-config }}
      root-config: ${{ steps.filter.outputs.root-config }}
      native-root-config: ${{ steps.filter.outputs.native-root-config }}
      speech: ${{ steps.filter.outputs.speech }}
      is-queue-leader: ${{ steps.queue-position.outputs.leader }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d # v4.0.3
        id: filter
        with:
          filters: |
            webapp:
              - 'packages/webapp/**'
            vfs-root:
              - 'packages/vfs-root/**'
            assets:
              - 'packages/assets/**'
            cloud-core:
              - 'packages/cloud-core/**'
            node-server:
              - 'packages/node-server/**'
            shared:
              - 'packages/shared-ts/**'
            webcomponents:
              - 'packages/webcomponents/**'
            spoon:
              - 'packages/spoon/**'
            extension:
              - 'packages/chrome-extension/**'
            cloudflare-worker:
              - 'packages/cloudflare-worker/**'
            cherry:
              - 'packages/cherry/**'
            dev-tools:
              - 'packages/dev-tools/**'
            # The CapabilityBroker REST contract (#2276). Replayed by BOTH
            # servers' suites, so a change to it alone must run both jobs —
            # the `shared` filter already pulls in node-server, this pulls in
            # swift-server, which would otherwise skip and leave the Swift
            # replay unrun.
            capability-contract:
              - 'packages/shared-ts/fixtures/capability-rest-contract.json'
            swift-server:
              - 'packages/swift-server/**'
            swift-optel:
              - 'packages/swift-optel/**'
            swift-traysession:
              - 'packages/swift-traysession/**'
            swift-trayfollower:
              - 'packages/swift-trayfollower/**'
            swift-traykit:
              - 'packages/swift-traykit/**'
            swift-widgetkit:
              - 'packages/swift-widgetkit/**'
            swift-launcher:
              - 'packages/swift-launcher/**'
            ios-app:
              - 'packages/ios-app/**'
              # The simulator-test toolchain and the two gates the iOS jobs
              # shell out to. Their vitest suites run on Linux and cover the
              # pure logic; only the macOS jobs feed the generated skip args to
              # `xcodebuild` or hand the drift gate a resolver-produced
              # lockfile, so a helper-only PR would ship those integrations
              # unexercised.
              - 'packages/dev-tools/tools/ios-sim-test.sh'
              - 'packages/dev-tools/tools/ios-sim-select.sh'
              - 'packages/dev-tools/tools/ios-ui-test-exclusions.mjs'
              - 'packages/dev-tools/tools/check-swift-resolved-drift.mjs'
              # A PR that rewrites the iOS CI steps used to get zero iOS
              # validation — only `swift-launcher` reran on a workflow edit,
              # via `native-root-config`. Scoped to the iOS filter rather than
              # `swift-config` on purpose: routing it through the filter every
              # native job consumes would make one latently-red Swift coverage
              # gate block every workflow edit in the repo.
              - '.github/workflows/ci.yml'
            go-optel:
              - 'packages/go-optel/**'
            slicc-cli:
              - 'packages/slicc-cli/**'
              # The Go corpus test decodes the shared golden fixture; a protocol
              # change regenerates it, so run the Go job when it moves too.
              - 'packages/ios-app/SliccFollower/Tests/SliccFollowerTests/Fixtures/tray-sync-corpus.json'
              # slicc-cli depends on go-optel via a local `replace` directive
              # (no go.work in this monorepo), so a go-optel-only change must
              # still run slicc-cli's suite.
              - 'packages/go-optel/**'
            swift-config:
              - '.swiftlint.yml'
              - '.swift-format'
              # Swift coverage floors + the gate script live outside the
              # swift packages, so a threshold-only ratchet PR must still
              # trigger the swift coverage jobs that consume them.
              - 'coverage-thresholds.json'
              - 'packages/dev-tools/tools/swift-coverage-check.sh'
              # Shared by every coverage job through swift-coverage-check.sh.
              - 'packages/dev-tools/tools/swift-coverage-runner-retry.sh'
            root-config:
              - 'package.json'
              - 'package-lock.json'
              - 'patches/**'
              - 'tsconfig*.json'
              - 'vitest*.config.*'
              - '.github/workflows/ci.yml'
              # Coverage floors + the TS gate tooling gate every package
              # job; a threshold-only ratchet PR must exercise them.
              - 'coverage-thresholds.json'
              - 'packages/dev-tools/tools/coverage-gate.mjs'
              - 'packages/dev-tools/tools/coverage-ratchet-lib.mjs'
            # The `root-config` set above is TS-shaped: tsconfigs, vitest
            # configs, the TS coverage gate, and the npm dependency graph.
            # None of it reaches a Swift compile, so the slow `macos-latest`
            # `swift-launcher` job uses this narrower set instead.
            # `patches/**` stays because patch-package rewrites the installed
            # node_modules that the bundled overlay build reads; the lockfile
            # does not, and a lockfile break surfaces on the fast ubuntu jobs
            # first.
            native-root-config:
              - 'patches/**'
              - '.github/workflows/ci.yml'
            # Real-model `say -o` WAV (gated; see the `e2e` job's speech leg).
            # Trigger on the synthesis engine, the `say` command, the
            # page-side panel RPC handlers that bridge it out of the
            # worker, and the terminal seam the test drives.
            speech:
              - 'packages/webapp/src/speech/**'
              - 'packages/webapp/src/shell/supplemental-commands/say-command.ts'
              - 'packages/webapp/src/ui/panel-rpc-handlers.ts'
              - 'packages/webapp/src/kernel/panel-rpc.ts'
              - 'packages/webapp/src/kernel/remote-terminal-view.ts'
              - 'packages/webapp/src/kernel/terminal-session-client.ts'
              - 'packages/webapp/src/ui/wc/wc-live.ts'
              - 'packages/webapp/tests/e2e/speech-roundtrip.test.ts'
              - 'packages/webapp/tests/e2e/helpers.ts'
              - 'packages/webapp/tests/e2e/playwright.config.ts'

      - name: Check merge queue position
        id: queue-position
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ "${{ github.event_name }}" != "merge_group" ]; then
            echo "leader=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          # Extract PR number from merge queue branch name (gh-readonly-queue/main/pr-NNN-sha)
          PR_NUMBER=$(echo "${{ github.event.merge_group.head_ref }}" | grep -oP 'pr-\K[0-9]+')
          if [ -z "$PR_NUMBER" ]; then
            echo "leader=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          POSITION=$(gh api graphql -f query='{ repository(owner: "${{ github.repository_owner }}", name: "${{ github.event.repository.name }}") { mergeQueue(branch: "main") { entries(first: 20) { nodes { position pullRequest { number } } } } } }' --jq ".data.repository.mergeQueue.entries.nodes[] | select(.pullRequest.number == $PR_NUMBER) | .position" 2>/dev/null || echo "")
          if [ "$POSITION" = "1" ] || [ -z "$POSITION" ]; then
            echo "leader=true" >> "$GITHUB_OUTPUT"
            echo "Queue position: $POSITION (leader)"
          else
            echo "leader=false" >> "$GITHUB_OUTPUT"
            echo "Queue position: $POSITION (skipping expensive jobs)"
          fi

  lint:
    runs-on: ubuntu-latest
    # Backstop, not a budget: a hung `npm ci` (see .github/actions/npm-ci)
    # must not sit on a runner until the 6-hour default fires.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          # Full history so the debt boy-scout gate
          # (check-touched-exemptions.mjs) can resolve merge-base
          # against $GITHUB_BASE_REF.
          fetch-depth: 0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - uses: ./.github/actions/npm-ci
      # Includes `lint:swift-deps`, the SPM unused-dependency gate. It parses
      # `packages/*/Package.swift` as text, so it runs here on ubuntu rather
      # than in the macOS Swift jobs — no toolchain required.
      - name: Lint (biome + prettier + CLAUDE.md sizes + tessl skill lint)
        run: npm run lint:ci
      - name: Autofix drift (npm run lint must be a no-op)
        # `biome check` above only fails on errors, but `npm run lint` runs
        # `biome check --write`, which applies every safe fix even for
        # warn/info rules. A violation of one of those is green here yet
        # rewritten by every local `npm run lint`, re-dirtying every worktree.
        # They land via commits that skip the pre-commit hook (GitHub web UI,
        # Copilot Autofix).
        run: npm run lint:autofix-drift
      - name: Debt boy-scout gate (biome rules + layer back-edges)
        # Fail when this PR touches a file that is still on a debt list —
        # a per-rule biome.json exemption list or the layer back-edge
        # baseline — without removing its entry. Skips on merge_group / push.
        run: node packages/dev-tools/tools/check-touched-exemptions.mjs
      - name: Check Chrome Web Store manifest justifications
        # Fail if the extension manifest gains/drops a permission without a
        # matching reviewer justification in docs/chrome-web-store-submission.md
        # (or vice-versa). Keeps the CWS submission pack in sync with the
        # shipped permission set.
        run: bash packages/dev-tools/tools/check-manifest-justifications.sh
      - name: Dead code detection (knip)
        # Hard-fail on unused files, missing/unlisted dependencies,
        # unresolved imports, duplicate exports, and unused binaries.
        # Unused individual exports/types are NOT enforced here — they
        # often represent intentional public API surface and are
        # tracked separately via 'npm run deadcode:full'.
        # Selector flags live in package.json so the CI gate cannot
        # silently diverge from the documented local command.
        run: npm run deadcode -- --no-progress --reporter compact
      - name: Dead code detection — production-mode files (knip)
        # Hard-fail on production-unreachable ("test-only") dead files
        # that the default gate above misses: knip's default mode treats
        # test files as entry points, so a module imported only by a
        # test (and otherwise wired to nothing) looks reachable. Running
        # knip with --production drops test entries, surfacing dead
        # files that exist solely to satisfy stale tests (precedent:
        # api-key-dialog.ts, issue #1018). Files-only by design —
        # individual unused exports remain intentional API surface and
        # are not enforced here. Selector flags live in package.json so
        # the CI gate cannot silently diverge from the documented local
        # command.
        run: npm run deadcode:production-files -- --no-progress --reporter compact

  # The merge queue lands PRs with a linear history (rebase, no merge
  # commits). A branch that merged `main` in — instead of rebasing onto it —
  # is rejected late, after the PR has already gone green and burned a full
  # CI run. Catch it here, at PR time. PR-only: in the merge_group the history
  # is linear by construction, so this is skipped (= passing).
  linear-history:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          # Full history so base..head can be walked for merge commits.
          fetch-depth: 0
      - name: Check linear history
        run: |
          bash packages/dev-tools/tools/check-linear-history.sh \
            "${{ github.event.pull_request.base.sha }}" \
            "${{ github.event.pull_request.head.sha }}"

  # Lightweight test matrix that re-runs the TypeScript test suites across
  # tested Node majors (currently 24, 25, and 26). The package engines
  # field is `>=22.13.0`, so any one-version pin in the heavier per-package
  # jobs misses regressions that only surface on newer runtimes (e.g. jsdom
  # + Node >= 25 quirks around vi.unstubAllGlobals + localStorage). Build/E2E
  # stay on a single Node version in the per-package jobs to keep CI time
  # bounded.
  # Full 9-project `tsc` matrix. The per-package jobs only run package-scoped
  # typechecks; the cross-project configs (tsconfig.webapp-worker.json, etc.)
  # were previously typechecked ONLY in the release job, so a type error there
  # merged green and then broke the release (bash-tool truncate import, #2020).
  # ~7s of tsc on top of npm ci — cheap insurance against that class of break.
  typecheck:
    needs: changes
    if: >-
      needs.changes.outputs.webapp == 'true' ||
      needs.changes.outputs.vfs-root == 'true' ||
      needs.changes.outputs.assets == 'true' ||
      needs.changes.outputs.cloud-core == 'true' ||
      needs.changes.outputs.node-server == 'true' ||
      needs.changes.outputs.shared == 'true' ||
      needs.changes.outputs.extension == 'true' ||
      needs.changes.outputs.cloudflare-worker == 'true' ||
      needs.changes.outputs.cherry == 'true' ||
      needs.changes.outputs.dev-tools == 'true' ||
      needs.changes.outputs.root-config == 'true'
    runs-on: ubuntu-latest
    # Backstop, not a budget: a hung `npm ci` (see .github/actions/npm-ci)
    # must not sit on a runner until the 6-hour default fires.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - uses: ./.github/actions/npm-ci
      - name: Typecheck (all tsc projects)
        run: npm run typecheck

  node-matrix-tests:
    needs: changes
    if: >-
      needs.changes.outputs.webapp == 'true' ||
      needs.changes.outputs.vfs-root == 'true' ||
      needs.changes.outputs.assets == 'true' ||
      needs.changes.outputs.cloud-core == 'true' ||
      needs.changes.outputs.node-server == 'true' ||
      needs.changes.outputs.shared == 'true' ||
      needs.changes.outputs.extension == 'true' ||
      needs.changes.outputs.cloudflare-worker == 'true' ||
      needs.changes.outputs.cherry == 'true' ||
      needs.changes.outputs.dev-tools == 'true' ||
      needs.changes.outputs.root-config == 'true'
    runs-on: ubuntu-latest
    # Backstop, not a budget: a hung `npm ci` (see .github/actions/npm-ci)
    # must not sit on a runner until the 6-hour default fires.
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        node-version: ['24', '25', '26']
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
      - uses: ./.github/actions/npm-ci
      - name: Vitest (all projects, no coverage thresholds)
        run: npm run test
        env:
          NODE_OPTIONS: '--max-old-space-size=6144'
          SLICC_CDP_LAUNCH_TIMEOUT_MS: '60000'

  webapp:
    needs: changes
    if: >-
      needs.changes.outputs.webapp == 'true' ||
      needs.changes.outputs.vfs-root == 'true' ||
      needs.changes.outputs.assets == 'true' ||
      needs.changes.outputs.shared == 'true' ||
      needs.changes.outputs.spoon == 'true' ||
      needs.changes.outputs.root-config == 'true'
    runs-on: ubuntu-latest
    # Backstop, not a budget: a hung `npm ci` (see .github/actions/npm-ci)
    # must not sit on a runner until the 6-hour default fires.
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - uses: ./.github/actions/npm-ci
      - name: Typecheck
        run: tsc --noEmit -p tsconfig.json
      - name: Test (with coverage threshold gate)
        run: npm run test:coverage:webapp
        env:
          NODE_OPTIONS: '--max-old-space-size=6144'
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-webapp
          path: coverage/
          if-no-files-found: ignore
      - name: Upload test timing report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-timing-webapp
          path: test-timing/
          if-no-files-found: ignore
      - name: Build
        run: npm run build -w @slicc/webapp
      - name: Check bundle for Node-only APIs
        run: |
          # Scan browser bundles for Node-only APIs that should never be bundled.
          # Excluded files:
          #   - pyodide:        legitimately contains Node references in dead branches
          #     (its IN_NODE detection gates the Node-only paths at runtime).
          #   - kernel-worker:  bundles pi-coding-agent's compaction module, which has
          #     the same IN_NODE detection pattern. The worker is a Web Worker
          #     context where IN_NODE is false; the Node branches are dead code at
          #     load time. Manually verified — see Phase 2 work.
          #   - py-realm-worker: hosts Pyodide, so by definition pulls the same
          #     IN_NODE-gated dead branches as pyodide-* itself.
          #   - transformers.web: @huggingface/transformers (the speech stack's
          #     whisper/kokoro engines) carries the same environment-gated Node
          #     compat branches (fileURLToPath etc.); in the browser its
          #     IS_NODE_ENV detection keeps them dead at load time.
          FORBIDDEN='fileURLToPath|require\("(fs|path|crypto|child_process|os|net|tls|http2|dgram|cluster|readline|repl|vm|v8|perf_hooks|worker_threads|inspector)"\)'
          MATCHES=$(grep -rlE "$FORBIDDEN" dist/ui/assets/*.js 2>/dev/null | grep -vE 'pyodide|kernel-worker|js-realm-worker|py-realm-worker|transformers\.web' || true)
          if [ -n "$MATCHES" ]; then
            echo "::error::Node-only APIs found in browser bundle:"
            for f in $MATCHES; do
              echo "  $f:"
              grep -nE "$FORBIDDEN" "$f" | head -5
            done
            exit 1
          fi
          echo "✓ No Node-only APIs found in webapp bundle"
      - name: Check dev deps for Node-only APIs
        working-directory: packages/webapp
        run: |
          # Force Vite to pre-bundle dependencies (same as dev server startup)
          npx vite optimize --force
          # Scan optimized deps for Node-only APIs that crash in the browser
          FORBIDDEN='fileURLToPath|require\("(fs|path|crypto|child_process|os|net|tls|http2|dgram|cluster|readline|repl|vm|v8|perf_hooks|worker_threads|inspector)"\)'
          # transformers.web: environment-gated Node compat branches, dead in
          # the browser (same justification as the bundle scans above).
          MATCHES=$(grep -rlE "$FORBIDDEN" ../../node_modules/.vite/deps/*.js 2>/dev/null | grep -vE 'pyodide|transformers\.web' || true)
          if [ -n "$MATCHES" ]; then
            echo "::error::Node-only APIs found in Vite dev-optimized deps:"
            for f in $MATCHES; do
              echo "  $f:"
              grep -nE "$FORBIDDEN" "$f" | head -5
            done
            exit 1
          fi
          echo "✓ No Node-only APIs found in dev-optimized deps"
  # Fake-LLM agent-loop E2E (Playwright): real WC composer + kernel-worker
  # agent against the deterministic fake LLM server, through the production
  # standalone topology. Split from `webapp` so the gate also bites on the
  # other two runtimes the harness drives end to end: node-server (the `/cdp`
  # bridge + `/api` surface the leader dials back to) and cloudflare-worker
  # (`wrangler dev` serves `dist/ui` exactly as production; the `/status`
  # readiness probe and `run_worker_first` invariants live in the worker).
  # Before the split, a node-server- or worker-only change never ran the E2E.
  #
  # The real-Kokoro speech round-trip (`say -o` WAV validation) rides the same
  # harness, so it runs here as a conditional leg instead of a separate job:
  # on `speech`-filter changes the run sets `RUN_REAL_SPEECH_E2E=1` (un-skipping
  # `speech-roundtrip.test.ts`) and first frees runner disk, because staging
  # the Kokoro-82M weights (~92 MB in OPFS) needs Chromium's per-origin storage
  # quota — derived from free disk — to not be the variable under test.
  e2e:
    needs: changes
    if: >-
      needs.changes.outputs.webapp == 'true' ||
      needs.changes.outputs.vfs-root == 'true' ||
      needs.changes.outputs.assets == 'true' ||
      needs.changes.outputs.shared == 'true' ||
      needs.changes.outputs.spoon == 'true' ||
      needs.changes.outputs.webcomponents == 'true' ||
      needs.changes.outputs.cloud-core == 'true' ||
      needs.changes.outputs.node-server == 'true' ||
      needs.changes.outputs.cloudflare-worker == 'true' ||
      needs.changes.outputs.root-config == 'true' ||
      needs.changes.outputs.speech == 'true'
    runs-on: ubuntu-latest
    # Budget covers setup + three builds + the full suite on one worker, and on
    # the speech leg the kokoro spec alone may take up to its 25-minute test
    # timeout (staging recovery = one page reboot + a second ~6-min download
    # pass). 30 could kill the job before the spec's own budget played out.
    # The multiple-cones leg (#2313) adds four specs whose own ceiling is 5
    # minutes each, hence 55 rather than 45 — on a speech run they and the
    # kokoro spec can both take their worst case.
    timeout-minutes: 55
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - uses: ./.github/actions/npm-ci
      - name: Build webapp (wrangler serves dist/ui)
        run: npm run build -w @slicc/webapp
      - name: Build @slicc/shared-ts (node-server runtime dependency)
        run: npm run build -w @slicc/shared-ts
      - name: Build node-server (the /cdp bridge under test)
        run: npm run build -w @slicc/node-server
      # Chromium derives a per-origin storage quota from FREE DISK on the
      # volume holding the browser profile. When the quota runs short the run
      # does not fail where the space ran out: the model still loads, then
      # synthesis reports an empty voice list. Drop the preinstalled
      # toolchains this job never uses (~20 GB); `df` is logged either side
      # to keep that claim checkable. Speech leg only — ordinary runs don't
      # pay the ~30 s of `rm`.
      - name: Free disk space for the browser storage quota
        if: needs.changes.outputs.speech == 'true'
        run: |
          df -h /
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc \
                      /opt/hostedtoolcache/CodeQL /usr/local/share/boost || true
          df -h /
      # Chromium for Playwright is ~170 MB per run; key on the pinned
      # @playwright/test version so a bump re-downloads and nothing else does.
      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            playwright-chromium-${{ runner.os }}-
      - name: Install Playwright Chromium
        run: |
          if [ "${{ steps.playwright-cache.outputs.cache-hit }}" = "true" ]; then
            npx playwright install-deps chromium
          else
            npx playwright install --with-deps chromium
          fi
      # The speech leg stages ~92 MB of Kokoro weights through the virtual
      # shell's `hf download`. Serve them from a disk-cached local mirror
      # (`HF_ENDPOINT`) so a warm run never touches the CDN; the store is keyed
      # on the spec, which pins the exact file list, with a prefix fallback.
      - name: Cache Hugging Face mirror store
        if: needs.changes.outputs.speech == 'true'
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .cache/hf-mirror
          key: hf-mirror-${{ hashFiles('packages/webapp/tests/e2e/speech-roundtrip.test.ts') }}
          restore-keys: |
            hf-mirror-
      - name: Start Hugging Face caching mirror
        if: needs.changes.outputs.speech == 'true'
        run: |
          mkdir -p .cache/hf-mirror
          nohup node packages/dev-tools/tools/hf-cache-mirror.mjs \
            --port 8791 --dir .cache/hf-mirror --ready-file .cache/hf-mirror.ready \
            > .cache/hf-mirror.log 2>&1 &
          for i in $(seq 1 50); do [ -f .cache/hf-mirror.ready ] && break; sleep 0.2; done
          test -f .cache/hf-mirror.ready || { cat .cache/hf-mirror.log; exit 1; }
          echo "HF_ENDPOINT=http://127.0.0.1:8791" >> "$GITHUB_ENV"
      - name: E2E tests
        # Includes the two-instance multiple-cones leg (#2313): the same
        # `wrangler dev` that serves the UI is the tray hub a second browser
        # context joins, so the leader/follower specs need no extra service —
        # only the per-spec `test.setTimeout` they set themselves, because CI is
        # far slower than a laptop at the object construction under a cone
        # lifecycle. No path gate: cone behaviour is a webapp-wide invariant.
        run: npm run test:e2e
        env:
          # Surface wrangler's own diagnostics so a crash during workerd
          # cold-start doesn't reduce to an empty `X [ERROR]` in the Playwright
          # webServer log. Combined with the `/status` readiness gate in the
          # Playwright config, this makes a startup race actually debuggable.
          WRANGLER_LOG: debug
          NODE_OPTIONS: '--max-old-space-size=6144'
          # Un-skip the real-Kokoro speech round-trip only when speech
          # surfaces changed; everywhere else the test self-skips.
          RUN_REAL_SPEECH_E2E: ${{ needs.changes.outputs.speech == 'true' && '1' || '' }}
      - name: Mirror log
        if: always() && needs.changes.outputs.speech == 'true'
        run: cat .cache/hf-mirror.log || true
      - name: Upload Playwright artifacts
        # Playwright's report + trace + screenshots are the primary post-mortem
        # surface when the E2E job flakes. Without them a failure reduces to a
        # single log line and a rerun; with them, the failing spec's trace
        # zip is one download away. Runs on failure and success — a green run
        # with a captured trace makes it easy to diff against a red one.
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: playwright-report-e2e
          # Playwright resolves both the HTML reporter's `outputFolder` and the
          # per-test `outputDir` against the nearest package root walked up from
          # the config file, i.e. `packages/webapp/` (the config sits at
          # `packages/webapp/tests/e2e/`). That is where `playwright-report/`
          # (HTML index + trace zips) and `test-results/` (screenshots, videos,
          # per-test artifacts) land — verified locally.
          path: |
            packages/webapp/playwright-report/
            packages/webapp/test-results/
            packages/webapp/tests/e2e/test-results/
          if-no-files-found: ignore
      - name: Show wrangler crash report
        # The supervisor (`packages/webapp/tests/e2e/wrangler-server.ts`) appends
        # one section per unexpected workerd exit — code, signal, and the tail of
        # its output. Printing it here means a mid-suite crash (#2372) is visible
        # in the job log itself, without downloading an artifact, even when the
        # Playwright retry made the job green.
        if: always()
        run: |
          if [ -f .wrangler/e2e-logs/crash-report.md ]; then
            echo '::warning title=WRANGLER_CRASHED::wrangler dev crashed during the e2e run - see the crash report below and the wrangler-logs-e2e artifact'
            cat .wrangler/e2e-logs/crash-report.md
          else
            echo 'No wrangler crash recorded.'
          fi
      - name: Upload wrangler logs
        # wrangler writes structured startup + request logs to
        # wrangler-YYYY-MM-DD_HH-MM-SS.log even when its stderr is captured by
        # another process; that file is where the real workerd bring-up stack
        # ends up when the process crashes. The Playwright config points
        # `WRANGLER_LOG_PATH` at `.wrangler/e2e-logs/` so those logs (plus the
        # supervisor's crash report) land in the workspace and can be uploaded;
        # the home-directory path stays listed for a wrangler invocation that
        # does not inherit the override.
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: wrangler-logs-e2e
          path: |
            .wrangler/e2e-logs/
            ~/.config/.wrangler/logs/
          if-no-files-found: ignore

  node-server:
    needs: changes
    if: >-
      needs.changes.outputs.cloud-core == 'true' ||
      needs.changes.outputs.node-server == 'true' ||
      needs.changes.outputs.shared == 'true' ||
      needs.changes.outputs.spoon == 'true' ||
      needs.changes.outputs.root-config == 'true'
    runs-on: ubuntu-latest
    # Backstop, not a budget: a hung `npm ci` (see .github/actions/npm-ci)
    # must not sit on a runner until the 6-hour default fires.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - uses: ./.github/actions/npm-ci
      - name: Typecheck (build config)
        run: tsc --noEmit -p tsconfig.cli.json
      - name: Typecheck (src + tests)
        run: tsc --noEmit -p packages/node-server/tsconfig.json
      - name: Test (with coverage threshold gate)
        run: npm run test:coverage:node-server
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-node-server
          path: coverage/
          if-no-files-found: ignore
      - name: Upload test timing report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-timing-node-server
          path: test-timing/
          if-no-files-found: ignore
      - name: Build @slicc/shared-ts (runtime dependency)
        run: npm run build -w @slicc/shared-ts
      - name: Build
        run: npm run build -w @slicc/node-server

  chrome-extension:
    needs: changes
    if: >-
      needs.changes.outputs.extension == 'true' ||
      needs.changes.outputs.webapp == 'true' ||
      needs.changes.outputs.vfs-root == 'true' ||
      needs.changes.outputs.assets == 'true' ||
      needs.changes.outputs.shared == 'true' ||
      needs.changes.outputs.spoon == 'true' ||
      needs.changes.outputs.root-config == 'true'
    runs-on: ubuntu-latest
    # Backstop, not a budget: a hung `npm ci` (see .github/actions/npm-ci)
    # must not sit on a runner until the 6-hour default fires.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - uses: ./.github/actions/npm-ci
      - name: Typecheck
        run: tsc --noEmit -p tsconfig.json
      - name: Test (with coverage threshold gate)
        run: npm run test:coverage:chrome-extension
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-chrome-extension
          path: coverage/
          if-no-files-found: ignore
      - name: Upload test timing report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-timing-chrome-extension
          path: test-timing/
          if-no-files-found: ignore
      - name: Build webapp (dependency)
        run: npm run build -w @slicc/webapp
      - name: Build extension
        run: npm run build -w @slicc/chrome-extension
      - name: Upload unpacked extension build
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: chrome-extension-unpacked
          path: dist/extension/
          if-no-files-found: error
      - name: Check bundle for forbidden CDN URL literals (MV3 RHC guard)
        run: bash packages/dev-tools/tools/check-extension-rhc.sh
      - name: Check bundle for Node-only APIs
        run: |
          # Scan extension bundles for Node-only APIs that should never be bundled.
          # Exclude pyodide files which legitimately contain Node references.
          FORBIDDEN='fileURLToPath|require\("(fs|path|crypto|child_process|os|net|tls|http2|dgram|cluster|readline|repl|vm|v8|perf_hooks|worker_threads|inspector)"\)'
          # Same exclusions as the webapp scan (see comment there).
          MATCHES=$(grep -rlE "$FORBIDDEN" dist/extension/*.js dist/extension/assets/*.js 2>/dev/null | grep -vE 'pyodide|kernel-worker|js-realm-worker|py-realm-worker|transformers\.web' || true)
          if [ -n "$MATCHES" ]; then
            echo "::error::Node-only APIs found in extension bundle:"
            for f in $MATCHES; do
              echo "  $f:"
              grep -nE "$FORBIDDEN" "$f" | head -5
            done
            exit 1
          fi
          echo "✓ No Node-only APIs found in extension bundle"
      - name: Check dev deps for Node-only APIs
        working-directory: packages/chrome-extension
        run: |
          # Force Vite to pre-bundle dependencies (same as dev server startup)
          npx vite optimize --force
          # Scan optimized deps for Node-only APIs that crash in the browser
          FORBIDDEN='fileURLToPath|require\("(fs|path|crypto|child_process|os|net|tls|http2|dgram|cluster|readline|repl|vm|v8|perf_hooks|worker_threads|inspector)"\)'
          # transformers.web: environment-gated Node compat branches, dead in
          # the browser (same justification as the bundle scans above).
          MATCHES=$(grep -rlE "$FORBIDDEN" ../../node_modules/.vite/deps/*.js 2>/dev/null | grep -vE 'pyodide|transformers\.web' || true)
          if [ -n "$MATCHES" ]; then
            echo "::error::Node-only APIs found in Vite dev-optimized deps (extension):"
            for f in $MATCHES; do
              echo "  $f:"
              grep -nE "$FORBIDDEN" "$f" | head -5
            done
            exit 1
          fi
          echo "✓ No Node-only APIs found in dev-optimized deps (extension)"
      - name: Install Chrome for Testing
        run: npx --yes @puppeteer/browsers install chrome@stable
      - name: Install xvfb
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends xvfb
      - name: Extension smoke test (CDP)
        env:
          SLICC_CI: '1'
        # MV3 extensions with side panels need headed Chrome; xvfb provides
        # the required display on a headless runner. The test is permitted
        # to fail loudly while the ffmpeg-core.js bundling work lands —
        # `continue-on-error: true` keeps CI green during the rollout but
        # surfaces the artifact so the breakage stays visible.
        continue-on-error: true
        run: xvfb-run -a npm run test:extension-smoke -w @slicc/chrome-extension

  cloudflare-worker:
    needs: changes
    if: >-
      needs.changes.outputs.cloud-core == 'true' ||
      needs.changes.outputs.cloudflare-worker == 'true' ||
      needs.changes.outputs.webapp == 'true' ||
      needs.changes.outputs.vfs-root == 'true' ||
      needs.changes.outputs.assets == 'true' ||
      needs.changes.outputs.root-config == 'true'
    runs-on: ubuntu-latest
    # Raised from 15: the job itself runs ~9 minutes, which left no room for
    # the npm-ci action to time out a hung install and retry it (~9 minutes).
    # A single hang would have failed the job instead of recovering from it.
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - uses: ./.github/actions/npm-ci
      - name: Build webapp for worker static assets
        run: npm run build -w @slicc/webapp
      # Hard gate: validate the worker bundle + static-asset constraints the
      # same way the release job does (`wrangler deploy --dry-run`). The
      # staging deploy steps below are `continue-on-error`, so without this a
      # webapp change that pushes a static asset past Cloudflare's 25 MiB
      # per-file cap would pass the PR and only break in release. Runs with no
      # secrets or network writes.
      - name: Validate worker bundle and asset sizes (dry-run)
        run: npm run build -w @slicc/cloudflare-worker
      - name: Typecheck
        run: tsc --noEmit -p tsconfig.worker.json
      - name: Test (with coverage threshold gate)
        run: npm run test:coverage:cloudflare-worker
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-cloudflare-worker
          path: coverage/
          if-no-files-found: ignore
      # Per-test timing data. Emitted by a second vitest invocation rather than
      # by the gate step above because coverage-gate.mjs builds its own argv
      # (floors from coverage-thresholds.json) and forwards nothing, and vitest
      # reporters are a global option with no per-project override. The worker
      # suite runs in ~3s, so the duplicate run is cheaper than plumbing a
      # reporter through the shared gate. Runs on failure too — a timing report
      # of the run that just broke is the useful one.
      - name: Emit test timing report
        if: always()
        continue-on-error: true
        run: npx vitest run --project cloudflare-worker --reporter=junit --outputFile.junit=test-results/cloudflare-worker-junit.xml
      - name: Upload test timing report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-timing-cloudflare-worker
          path: test-results/
          if-no-files-found: ignore

      # #1330 retention: archive this build's content-hashed assets to the
      # STAGING R2 bucket BEFORE any deploy attempt so long-lived tabs can
      # recover gone lazy chunks. Hard-fail gate (NOT continue-on-error) — a
      # build must never go live unarchived. Shares the deploy step's non-fork
      # guard so it never runs without Cloudflare secrets on fork PRs.
      - name: Archive assets to R2 (staging)
        if: |
          (github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork == false) ||
          github.event_name == 'push'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
        run: node packages/cloudflare-worker/scripts/upload-assets-to-r2.mjs slicc-asset-archive-staging --dir dist/ui/assets
      # Deploy to staging on PRs (not forks) and pushes to main.
      #
      # The staging Worker (slicc-tray-hub-staging) has Worker Versions enabled.
      # wrangler-action runs `wrangler secret bulk` BEFORE the `command:`
      # whenever `secrets:` is set, so a single deploy+secrets step uploads
      # secrets first. When the Worker's latest version is not the deployed one
      # (a pending/undeployed version exists), `secret bulk` fails with
      # Cloudflare error 10215 ("deploy the latest version first, then modify
      # secrets"). Each attempt is therefore split into two invocations:
      # Step A deploys the latest version (no `secrets:`), clearing the
      # pending-version state and emitting the `deployment-url`; Step B then
      # uploads secrets, which now succeeds because the latest version is
      # deployed. The action has no channel to feed values to
      # `versions secret bulk` (the `secrets:` input is hard-wired to
      # `secret bulk`), so this "deploy first, then modify secrets" workaround
      # is used instead of the versions secret API. `environment: staging` is
      # required on both so wrangler targets slicc-tray-hub-staging, not the
      # production Worker.
      - name: Deploy staging worker (attempt 1)
        id: deploy_attempt_1
        if: |
          (github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork == false) ||
          github.event_name == 'push'
        continue-on-error: true
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: packages/cloudflare-worker
          environment: staging
          command: deploy --env staging
      - name: Upload staging secrets (attempt 1)
        id: deploy_attempt_1_secrets
        if: |
          (github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork == false) ||
          github.event_name == 'push'
        continue-on-error: true
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: packages/cloudflare-worker
          environment: staging
          command: deploy --env staging
          secrets: |
            CLOUDFLARE_TURN_API_TOKEN
            GITHUB_CLIENT_SECRET
            E2B_API_KEY
        env:
          CLOUDFLARE_TURN_API_TOKEN: ${{ secrets.CLOUDFLARE_TURN_API_TOKEN }}
          GITHUB_CLIENT_SECRET: ${{ secrets.OAUTH_GITHUB_SECRET_STAGING }}
          E2B_API_KEY: ${{ secrets.E2B_API_KEY }}
      - name: Wait before staging deploy retry 2
        if: steps.deploy_attempt_1.outcome == 'failure' || steps.deploy_attempt_1_secrets.outcome == 'failure'
        run: sleep 20
      - name: Deploy staging worker (attempt 2)
        id: deploy_attempt_2
        if: steps.deploy_attempt_1.outcome == 'failure' || steps.deploy_attempt_1_secrets.outcome == 'failure'
        continue-on-error: true
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: packages/cloudflare-worker
          environment: staging
          command: deploy --env staging
      - name: Upload staging secrets (attempt 2)
        id: deploy_attempt_2_secrets
        if: steps.deploy_attempt_1.outcome == 'failure' || steps.deploy_attempt_1_secrets.outcome == 'failure'
        continue-on-error: true
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: packages/cloudflare-worker
          environment: staging
          command: deploy --env staging
          secrets: |
            CLOUDFLARE_TURN_API_TOKEN
            GITHUB_CLIENT_SECRET
            E2B_API_KEY
        env:
          CLOUDFLARE_TURN_API_TOKEN: ${{ secrets.CLOUDFLARE_TURN_API_TOKEN }}
          GITHUB_CLIENT_SECRET: ${{ secrets.OAUTH_GITHUB_SECRET_STAGING }}
          E2B_API_KEY: ${{ secrets.E2B_API_KEY }}
      - name: Wait before staging deploy retry 3
        if: (steps.deploy_attempt_1.outcome == 'failure' || steps.deploy_attempt_1_secrets.outcome == 'failure') && (steps.deploy_attempt_2.outcome == 'failure' || steps.deploy_attempt_2_secrets.outcome == 'failure')
        run: sleep 40
      - name: Deploy staging worker (attempt 3)
        id: deploy_attempt_3
        if: (steps.deploy_attempt_1.outcome == 'failure' || steps.deploy_attempt_1_secrets.outcome == 'failure') && (steps.deploy_attempt_2.outcome == 'failure' || steps.deploy_attempt_2_secrets.outcome == 'failure')
        continue-on-error: true
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: packages/cloudflare-worker
          environment: staging
          command: deploy --env staging
      - name: Upload staging secrets (attempt 3)
        id: deploy_attempt_3_secrets
        if: (steps.deploy_attempt_1.outcome == 'failure' || steps.deploy_attempt_1_secrets.outcome == 'failure') && (steps.deploy_attempt_2.outcome == 'failure' || steps.deploy_attempt_2_secrets.outcome == 'failure')
        continue-on-error: true
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: packages/cloudflare-worker
          environment: staging
          command: deploy --env staging
          secrets: |
            CLOUDFLARE_TURN_API_TOKEN
            GITHUB_CLIENT_SECRET
            E2B_API_KEY
        env:
          CLOUDFLARE_TURN_API_TOKEN: ${{ secrets.CLOUDFLARE_TURN_API_TOKEN }}
          GITHUB_CLIENT_SECRET: ${{ secrets.OAUTH_GITHUB_SECRET_STAGING }}
          E2B_API_KEY: ${{ secrets.E2B_API_KEY }}
      - name: Finalize staging deploy
        id: deploy
        if: (steps.deploy_attempt_1.outcome == 'success' && steps.deploy_attempt_1_secrets.outcome == 'success') || (steps.deploy_attempt_2.outcome == 'success' && steps.deploy_attempt_2_secrets.outcome == 'success') || (steps.deploy_attempt_3.outcome == 'success' && steps.deploy_attempt_3_secrets.outcome == 'success')
        env:
          DEPLOY_A_1: ${{ steps.deploy_attempt_1.outcome }}
          DEPLOY_B_1: ${{ steps.deploy_attempt_1_secrets.outcome }}
          DEPLOYMENT_URL_1: ${{ steps.deploy_attempt_1.outputs.deployment-url }}
          DEPLOY_A_2: ${{ steps.deploy_attempt_2.outcome }}
          DEPLOY_B_2: ${{ steps.deploy_attempt_2_secrets.outcome }}
          DEPLOYMENT_URL_2: ${{ steps.deploy_attempt_2.outputs.deployment-url }}
          DEPLOY_A_3: ${{ steps.deploy_attempt_3.outcome }}
          DEPLOY_B_3: ${{ steps.deploy_attempt_3_secrets.outcome }}
          DEPLOYMENT_URL_3: ${{ steps.deploy_attempt_3.outputs.deployment-url }}
        run: |
          DEPLOYMENT_URL=""
          if [ "$DEPLOY_A_1" = "success" ] && [ "$DEPLOY_B_1" = "success" ]; then DEPLOYMENT_URL="${DEPLOYMENT_URL_1:-}"; fi
          if [ -z "$DEPLOYMENT_URL" ] && [ "$DEPLOY_A_2" = "success" ] && [ "$DEPLOY_B_2" = "success" ]; then DEPLOYMENT_URL="${DEPLOYMENT_URL_2:-}"; fi
          if [ -z "$DEPLOYMENT_URL" ] && [ "$DEPLOY_A_3" = "success" ] && [ "$DEPLOY_B_3" = "success" ]; then DEPLOYMENT_URL="${DEPLOYMENT_URL_3:-}"; fi
          if [ -z "$DEPLOYMENT_URL" ]; then
            echo "::error::Staging deploy failed after 3 attempts"
            exit 1
          fi
          echo "deployment-url=${DEPLOYMENT_URL}" >> "$GITHUB_OUTPUT"
      - name: Smoke test staging
        if: |
          (github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork == false) ||
          github.event_name == 'push'
        env:
          WORKER_BASE_URL: ${{ steps.deploy.outputs.deployment-url }}
          SLICC_ARCHIVE_SMOKE: '1'
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          SLICC_ARCHIVE_BUCKET: slicc-asset-archive-staging
        run: |
          for attempt in 1 2 3 4 5 6; do
            if npx vitest run --project cloudflare-worker packages/cloudflare-worker/tests/deployed.test.ts; then
              exit 0
            fi
            if [ "$attempt" -eq 6 ]; then
              echo "Deployed smoke test failed after ${attempt} attempts"
              exit 1
            fi
            echo "Smoke test attempt ${attempt} failed; waiting for propagation..."
            sleep 15
          done

      # Deploy the dedicated preview worker (no secrets, no static assets).
      # Runs after the main worker so the TRAY_HUB DO (referenced via
      # script_name) is up to date.
      - name: Deploy staging preview worker
        if: steps.deploy.outcome == 'success'
        continue-on-error: true
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: packages/cloudflare-worker
          environment: staging
          command: deploy --env staging --config wrangler-preview.jsonc

  cloud-core:
    needs: changes
    if: >-
      needs.changes.outputs.cloud-core == 'true' ||
      needs.changes.outputs.root-config == 'true'
    runs-on: ubuntu-latest
    # Backstop, not a budget: a hung `npm ci` (see .github/actions/npm-ci)
    # must not sit on a runner until the 6-hour default fires.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - uses: ./.github/actions/npm-ci
      - name: Typecheck
        run: tsc --noEmit -p packages/cloud-core/tsconfig.json
      - name: Test (with coverage threshold gate)
        run: npm run test:coverage:cloud-core
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-cloud-core
          path: coverage/
          if-no-files-found: ignore
      - name: Build
        run: npm run build -w @slicc/cloud-core

  go-optel:
    needs: changes
    if: >-
      needs.changes.outputs.go-optel == 'true' ||
      needs.changes.outputs.root-config == 'true'
    # Pure stdlib string/JSON logic with no OS-specific paths, unlike
    # slicc-cli's process-group signalling — one Linux job covers it.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: packages/go-optel/go.mod
          # No go.sum: the module has zero third-party dependencies.
          cache: false
      - name: Install golangci-lint
        run: |
          curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/v2.12.2/install.sh \
            | sh -s -- -b "$(go env GOPATH)/bin" v2.12.2
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Format, lint, vet, race tests, coverage floor
        working-directory: packages/go-optel
        run: make check

  slicc-cli:
    needs: changes
    if: >-
      needs.changes.outputs.slicc-cli == 'true' ||
      needs.changes.outputs.root-config == 'true'
    # The follower runs on macOS/Linux/Windows, so its test suite runs on all three
    # to exercise the real per-OS runtime paths (process-group signalling, the
    # cmd.exe vs sh runner) — a compile-only check would miss those. The static
    # gate + coverage floor run once on Linux: they are platform-independent, and
    # the floor would under-count where the POSIX-only tests skip.
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: packages/slicc-cli/go.mod
          cache-dependency-path: packages/slicc-cli/go.sum
      # macOS / Windows: run the suite to exercise the platform runtime paths.
      - name: Tests
        if: matrix.os != 'ubuntu-latest'
        working-directory: packages/slicc-cli
        run: go test ./...
      # Linux: the full gate (gofmt + vet + golangci-lint + race tests + coverage
      # floor) plus the cross-compile of every release target.
      - name: Install golangci-lint
        if: matrix.os == 'ubuntu-latest'
        run: |
          curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/v2.12.2/install.sh \
            | sh -s -- -b "$(go env GOPATH)/bin" v2.12.2
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - name: Format, lint, vet, race tests, coverage floor
        if: matrix.os == 'ubuntu-latest'
        working-directory: packages/slicc-cli
        run: make check
      # Per-test timings for the whole matrix: `go test -json` records each
      # test's elapsed time, so a regression shows up per-OS. No retry wrapper
      # here on purpose — every Go test in this module is hermetic (no network,
      # no real signaling server; the WebRTC integration test drives an in-process
      # leader harness), so a failure is a real failure and retrying would only
      # hide it.
      - name: Test timings (go test -json)
        if: always()
        working-directory: packages/slicc-cli
        run: make test-json
      - name: Upload test timings
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-timings-slicc-cli-${{ matrix.os }}
          path: packages/slicc-cli/test-report.json
          if-no-files-found: ignore
      - name: Cross-compile all targets (mac/linux/windows × amd64/arm64)
        if: matrix.os == 'ubuntu-latest'
        working-directory: packages/slicc-cli
        run: make dist

  shared:
    needs: changes
    if: >-
      needs.changes.outputs.shared == 'true' ||
      needs.changes.outputs.root-config == 'true'
    runs-on: ubuntu-latest
    # Backstop, not a budget: a hung `npm ci` (see .github/actions/npm-ci)
    # must not sit on a runner until the 6-hour default fires.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - uses: ./.github/actions/npm-ci
      - name: Typecheck
        run: npm run typecheck -w @slicc/shared-ts
      - name: Test (with coverage threshold gate)
        run: npm run test:coverage:shared
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-shared
          path: coverage/
          if-no-files-found: ignore
      - name: Build
        run: npm run build -w @slicc/shared-ts

  webcomponents:
    needs: changes
    if: >-
      needs.changes.outputs.webcomponents == 'true' ||
      needs.changes.outputs.spoon == 'true' ||
      needs.changes.outputs.root-config == 'true'
    runs-on: ubuntu-latest
    # Backstop, not a budget: a hung `npm ci` (see .github/actions/npm-ci)
    # must not sit on a runner until the 6-hour default fires.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - uses: ./.github/actions/npm-ci
      - name: Typecheck
        run: npm run typecheck -w @slicc/webcomponents
      # Functional tests run in real Chromium via the Playwright provider.
      - name: Install Chromium (browser tests)
        run: npx playwright install chromium
      - name: Test (with coverage threshold gate)
        run: npm run test:coverage:webcomponents
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-webcomponents
          path: packages/webcomponents/coverage/
          if-no-files-found: ignore
      - name: Build
        run: npm run build -w @slicc/webcomponents

  spoon:
    needs: changes
    if: >-
      needs.changes.outputs.spoon == 'true' ||
      needs.changes.outputs.root-config == 'true'
    runs-on: ubuntu-latest
    # Backstop, not a budget: a hung `npm ci` (see .github/actions/npm-ci)
    # must not sit on a runner until the 6-hour default fires.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - uses: ./.github/actions/npm-ci
      - name: Typecheck
        run: npm run typecheck -w @ai-ecoverse/spoon
      # The launcher is a real custom element — functional tests run in real
      # Chromium via the Playwright provider (mirrors @slicc/webcomponents).
      - name: Install Chromium (browser tests)
        run: npx playwright install chromium
      - name: Test (with coverage threshold gate)
        run: npm run test:coverage:spoon
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-spoon
          path: packages/spoon/coverage/
          if-no-files-found: ignore
      - name: Build
        run: npm run build -w @ai-ecoverse/spoon

  cherry:
    needs: changes
    if: >-
      needs.changes.outputs.cherry == 'true' ||
      needs.changes.outputs.root-config == 'true'
    runs-on: ubuntu-latest
    # Backstop, not a budget: a hung `npm ci` (see .github/actions/npm-ci)
    # must not sit on a runner until the 6-hour default fires.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - uses: ./.github/actions/npm-ci
      - name: Typecheck
        run: tsc --noEmit -p packages/cherry/tsconfig.json
      - name: Test (with coverage threshold gate)
        run: npm run test:coverage:cherry
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-cherry
          path: coverage/
          if-no-files-found: ignore
      - name: Build
        run: npm run build -w @ai-ecoverse/cherry

  swift-server:
    needs: [changes, lint]
    if: >-
      needs.lint.result == 'success' &&
      needs.changes.outputs.is-queue-leader == 'true' &&
      (needs.changes.outputs.swift-server == 'true' ||
      needs.changes.outputs.swift-trayfollower == 'true' ||
      needs.changes.outputs.swift-traykit == 'true' ||
      needs.changes.outputs.capability-contract == 'true' ||
      needs.changes.outputs.swift-config == 'true')
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      # swift-coverage-check.sh reads its floors from coverage-thresholds.json
      # via node, so pin Node (matches the other jobs) instead of relying on
      # the runner's preinstalled version.
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
      - name: Install SwiftLint
        run: brew install swiftlint
      - name: Lint (SwiftLint)
        run: cd packages/swift-server && swiftlint lint --reporter github-actions-logging
      # swift-format ships with the toolchain; config is the root .swift-format.
      - name: Lint (swift-format)
        run: cd packages/swift-server && swift format lint --strict --parallel --recursive Sources Tests Package.swift
      - name: Build
        run: cd packages/swift-server && swift build -c release
      - name: Test (with coverage threshold gate)
        run: |
          ./packages/dev-tools/tools/swift-coverage-check.sh \
            packages/swift-server SliccServerPackageTests
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-swift-server
          path: packages/swift-server/.build/coverage/
          if-no-files-found: ignore
      - name: Upload test timings
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-timings-swift-server
          path: packages/swift-server/.build/coverage/test-timings*
          if-no-files-found: ignore
      - name: Install Periphery
        run: brew install peripheryapp/periphery/periphery
      - name: Dead code detection (Periphery)
        run: |
          # Informational scan; surfaces unused Swift declarations without
          # blocking PRs while existing findings get cleaned up incrementally.
          cd packages/swift-server && periphery scan --disable-update-check || true

  swift-optel:
    needs: [changes, lint]
    if: >-
      needs.lint.result == 'success' &&
      needs.changes.outputs.is-queue-leader == 'true' &&
      (needs.changes.outputs.swift-optel == 'true' ||
      needs.changes.outputs.swift-config == 'true')
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      # swift-coverage-check.sh reads its floors from coverage-thresholds.json
      # via node, so pin Node (matches the other jobs) instead of relying on
      # the runner's preinstalled version.
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
      - name: Install SwiftLint
        run: brew install swiftlint
      - name: Lint (SwiftLint)
        run: cd packages/swift-optel && swiftlint lint --reporter github-actions-logging
      # swift-format ships with the toolchain; config is the root .swift-format.
      - name: Lint (swift-format)
        run: cd packages/swift-optel && swift format lint --strict --parallel --recursive Sources Tests Package.swift
      - name: Build
        run: cd packages/swift-optel && swift build -c release
      - name: Test (with coverage threshold gate)
        run: |
          ./packages/dev-tools/tools/swift-coverage-check.sh \
            packages/swift-optel SwiftOptelPackageTests
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-swift-optel
          path: packages/swift-optel/.build/coverage/
          if-no-files-found: ignore
      - name: Upload test timings
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-timings-swift-optel
          path: packages/swift-optel/.build/coverage/test-timings*
          if-no-files-found: ignore
      - name: Install Periphery
        run: brew install peripheryapp/periphery/periphery
      - name: Dead code detection (Periphery)
        run: |
          # Informational scan; surfaces unused Swift declarations without
          # blocking PRs while existing findings get cleaned up incrementally.
          cd packages/swift-optel && periphery scan --disable-update-check || true

  swift-traysession:
    needs: [changes, lint]
    if: >-
      needs.lint.result == 'success' &&
      needs.changes.outputs.is-queue-leader == 'true' &&
      (needs.changes.outputs.swift-traysession == 'true' ||
      needs.changes.outputs.swift-config == 'true')
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      # swift-coverage-check.sh reads its floors from coverage-thresholds.json
      # via node, so pin Node (matches the other jobs) instead of relying on
      # the runner's preinstalled version.
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
      - name: Install SwiftLint
        run: brew install swiftlint
      - name: Lint (SwiftLint)
        run: cd packages/swift-traysession && swiftlint lint --reporter github-actions-logging
      # swift-format ships with the toolchain; config is the root .swift-format.
      - name: Lint (swift-format)
        run: cd packages/swift-traysession && swift format lint --strict --parallel --recursive Sources Tests Package.swift
      - name: Build
        run: cd packages/swift-traysession && swift build -c release
      # The package is consumed by iOS (SliccFollower) too; a macOS-only build
      # would let an AppKit import or macOS-only Foundation API slip through.
      - name: Build (iOS Simulator)
        run: |
          cd packages/swift-traysession
          xcodebuild build -scheme SliccTraySession \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath .build/xcodebuild-ios
      - name: Test (with coverage threshold gate)
        run: |
          ./packages/dev-tools/tools/swift-coverage-check.sh \
            packages/swift-traysession SliccTraySessionPackageTests
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-swift-traysession
          path: packages/swift-traysession/.build/coverage/
          if-no-files-found: ignore
      - name: Upload test timings
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-timings-swift-traysession
          path: packages/swift-traysession/.build/coverage/test-timings*
          if-no-files-found: ignore
      - name: Install Periphery
        run: brew install peripheryapp/periphery/periphery
      - name: Dead code detection (Periphery)
        run: |
          # Informational scan; surfaces unused Swift declarations without
          # blocking PRs while existing findings get cleaned up incrementally.
          cd packages/swift-traysession && periphery scan --disable-update-check || true

  swift-trayfollower:
    needs: [changes, lint]
    if: >-
      needs.lint.result == 'success' &&
      needs.changes.outputs.is-queue-leader == 'true' &&
      (needs.changes.outputs.swift-trayfollower == 'true' ||
      needs.changes.outputs.swift-config == 'true')
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      # swift-coverage-check.sh reads its floors from coverage-thresholds.json
      # via node, so pin Node (matches the other jobs).
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
      - name: Install SwiftLint
        run: brew install swiftlint
      - name: Lint (SwiftLint)
        run: cd packages/swift-trayfollower && swiftlint lint --reporter github-actions-logging
      # swift-format ships with the toolchain; config is the root .swift-format.
      - name: Lint (swift-format)
        run: cd packages/swift-trayfollower && swift format lint --strict --parallel --recursive Sources Tests Package.swift
      - name: Build
        run: cd packages/swift-trayfollower && swift build -c release
      # The package is consumed by iOS (SliccFollower) too; a macOS-only build
      # would let an AppKit import or macOS-only Foundation API slip through.
      - name: Build (iOS Simulator)
        run: |
          cd packages/swift-trayfollower
          xcodebuild build -scheme SliccTrayFollower \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath .build/xcodebuild-ios
      - name: Test (with coverage threshold gate)
        run: |
          ./packages/dev-tools/tools/swift-coverage-check.sh \
            packages/swift-trayfollower SliccTrayFollowerPackageTests
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-swift-trayfollower
          path: packages/swift-trayfollower/.build/coverage/
          if-no-files-found: ignore
      - name: Upload test timings
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-timings-swift-trayfollower
          path: packages/swift-trayfollower/.build/coverage/test-timings*
          if-no-files-found: ignore
      - name: Install Periphery
        run: brew install peripheryapp/periphery/periphery
      - name: Dead code detection (Periphery)
        run: |
          # Informational scan; surfaces unused Swift declarations without
          # blocking PRs while existing findings get cleaned up incrementally.
          cd packages/swift-trayfollower && periphery scan --disable-update-check || true

  swift-widgetkit:
    needs: [changes, lint]
    if: >-
      needs.lint.result == 'success' &&
      needs.changes.outputs.is-queue-leader == 'true' &&
      (needs.changes.outputs.swift-widgetkit == 'true' ||
      needs.changes.outputs.swift-config == 'true')
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      # swift-coverage-check.sh reads its floors from coverage-thresholds.json
      # via node, so pin Node (matches the other jobs).
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
      - name: Install SwiftLint
        run: brew install swiftlint
      - name: Lint (SwiftLint)
        run: cd packages/swift-widgetkit && swiftlint lint --reporter github-actions-logging
      # swift-format ships with the toolchain; config is the root .swift-format.
      - name: Lint (swift-format)
        run: cd packages/swift-widgetkit && swift format lint --strict --parallel --recursive Sources Tests Package.swift
      - name: Build
        run: cd packages/swift-widgetkit && swift build -c release
      # Both widget extensions build against this package; a macOS-only build
      # would let an AppKit import or a macOS-only WidgetKit family through and
      # only fail later in the (much slower) ios-app job.
      - name: Build (iOS Simulator)
        run: |
          cd packages/swift-widgetkit
          xcodebuild build -scheme SliccWidgetKit \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath .build/xcodebuild-ios
      - name: Test (with coverage threshold gate)
        run: |
          ./packages/dev-tools/tools/swift-coverage-check.sh \
            packages/swift-widgetkit SliccWidgetKitPackageTests
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-swift-widgetkit
          path: packages/swift-widgetkit/.build/coverage/
          if-no-files-found: ignore
      - name: Upload test timings
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-timings-swift-widgetkit
          path: packages/swift-widgetkit/.build/coverage/test-timings*
          if-no-files-found: ignore
      - name: Install Periphery
        run: brew install peripheryapp/periphery/periphery
      - name: Dead code detection (Periphery)
        run: |
          # Informational scan; surfaces unused Swift declarations without
          # blocking PRs while existing findings get cleaned up incrementally.
          cd packages/swift-widgetkit && periphery scan --disable-update-check || true

  swift-traykit:
    needs: [changes, lint]
    if: >-
      needs.lint.result == 'success' &&
      needs.changes.outputs.is-queue-leader == 'true' &&
      (needs.changes.outputs.swift-traykit == 'true' ||
      needs.changes.outputs.swift-config == 'true')
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
      - name: Install SwiftLint
        run: brew install swiftlint
      - name: Lint (SwiftLint)
        run: cd packages/swift-traykit && swiftlint lint --reporter github-actions-logging
      - name: Lint (swift-format)
        run: cd packages/swift-traykit && swift format lint --strict --parallel --recursive Sources Tests Package.swift
      - name: Build
        run: cd packages/swift-traykit && swift build -c release
      - name: Build (iOS Simulator)
        run: |
          cd packages/swift-traykit
          xcodebuild build -scheme SliccTrayVFS \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath .build/xcodebuild-ios
      - name: Test (with coverage threshold gate)
        run: |
          ./packages/dev-tools/tools/swift-coverage-check.sh \
            packages/swift-traykit SliccTrayVFSPackageTests
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-swift-traykit
          path: packages/swift-traykit/.build/coverage/
          if-no-files-found: ignore
      - name: Upload test timings
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-timings-swift-traykit
          path: packages/swift-traykit/.build/coverage/test-timings*
          if-no-files-found: ignore
      - name: Install Periphery
        run: brew install peripheryapp/periphery/periphery
      - name: Dead code detection (Periphery)
        run: |
          cd packages/swift-traykit && periphery scan --disable-update-check || true

  swift-launcher:
    needs: [changes, lint]
    # The launcher no longer bundles the webapp (`dist/ui`) — the UI loads from
    # the hosted origin — so webapp/vfs-root changes no longer trigger this slow
    # macos-latest job. The ONE web artifact it still embeds is the overlay
    # bootstrap, which now lives in the small `@ai-ecoverse/spoon` package, so a
    # `packages/spoon/**` change DOES trigger this job (and only that, not a
    # general webapp UI change). Assets still matter: `assemble-app.mjs` reads the
    # macOS icon from `packages/assets/`, so an asset-only PR must still build
    # Sliccstart.app. A pure webapp/vfs-root PR now skips swift-launcher entirely
    # (skipped = passing in the `ci` summary gate). The root-config trigger is
    # the narrower `native-root-config`: a tsconfig/vitest/lockfile-only PR
    # (mostly renovate bumps) cannot change a Swift compile, and the npm side of
    # this job is validated by the fast ubuntu jobs first.
    if: >-
      needs.lint.result == 'success' &&
      needs.changes.outputs.is-queue-leader == 'true' &&
      (needs.changes.outputs.swift-launcher == 'true' ||
      needs.changes.outputs.swift-server == 'true' ||
      needs.changes.outputs.swift-optel == 'true' ||
      needs.changes.outputs.swift-traysession == 'true' ||
      needs.changes.outputs.swift-traykit == 'true' ||
      needs.changes.outputs.swift-trayfollower == 'true' ||
      needs.changes.outputs.swift-widgetkit == 'true' ||
      needs.changes.outputs.swift-config == 'true' ||
      needs.changes.outputs.spoon == 'true' ||
      needs.changes.outputs.assets == 'true' ||
      needs.changes.outputs.native-root-config == 'true')
    runs-on: macos-latest
    # Backstop, not a budget: a hung `npm ci` (see .github/actions/npm-ci)
    # must not sit on a runner until the 6-hour default fires.
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - uses: ./.github/actions/npm-ci
      - name: Install SwiftLint
        run: brew install swiftlint
      # Artifacts from a mismatched toolchain are the classic way an SPM cache
      # turns a green build red, and `macos-latest` rolls its Xcode without
      # notice — so the toolchain identity is part of the cache key, not a hope.
      - name: Record Swift toolchain fingerprint
        id: swift-toolchain
        run: echo "id=$(swift --version 2>&1 | shasum -a 256 | cut -c1-16)" >> "$GITHUB_OUTPUT"
      # This job builds swift-server from scratch on every run — ~6 of its ~10
      # minutes — even when swift-server itself is unchanged and its own job is
      # skipped. Keying `.build` on the Swift sources plus the toolchain makes
      # the common case incremental. `coverage/` is excluded so a restored cache
      # can never hand a stale profile to the upload steps below.
      - name: Cache Swift build artifacts
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            packages/swift-launcher/.build
            !packages/swift-launcher/.build/coverage
            packages/swift-server/.build
          key: swift-launcher-build-${{ runner.os }}-${{ runner.arch }}-${{ steps.swift-toolchain.outputs.id }}-${{ hashFiles('packages/swift-launcher/Package.*', 'packages/swift-server/Package.*', 'packages/swift-optel/Package.*', 'packages/swift-traysession/Package.*', 'packages/swift-traykit/Package.*') }}-${{ hashFiles('packages/swift-launcher/Sliccstart/**', 'packages/swift-launcher/SliccstartTests/**', 'packages/swift-launcher/SliccFileProvider/**', 'packages/swift-server/Sources/**', 'packages/swift-server/Tests/**', 'packages/swift-optel/Sources/**', 'packages/swift-traysession/Sources/**', 'packages/swift-traykit/Sources/**') }}
          restore-keys: |
            swift-launcher-build-${{ runner.os }}-${{ runner.arch }}-${{ steps.swift-toolchain.outputs.id }}-${{ hashFiles('packages/swift-launcher/Package.*', 'packages/swift-server/Package.*', 'packages/swift-optel/Package.*', 'packages/swift-traysession/Package.*', 'packages/swift-traykit/Package.*') }}-
      - name: Lint (SwiftLint)
        run: cd packages/swift-launcher && swiftlint lint --reporter github-actions-logging
      # swift-format ships with the toolchain; config is the root .swift-format.
      - name: Lint (swift-format)
        run: cd packages/swift-launcher && swift format lint --strict --parallel --recursive Sliccstart SliccstartTests Package.swift
      - name: Build swift-server (dependency)
        run: cd packages/swift-server && swift build -c release
      - name: Test (with coverage threshold gate)
        env:
          # UpdateCheckIntegrationTests hits the real GitHub API.
          # Authenticate to lift the 60/hr unauth limit (which is shared per
          # runner IP and routinely exhausted on `macos-latest`) up to the
          # 5,000/hr authenticated limit.
          GH_TOKEN: ${{ github.token }}
        run: |
          ./packages/dev-tools/tools/swift-coverage-check.sh \
            packages/swift-launcher SliccstartPackageTests
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-swift-launcher
          path: packages/swift-launcher/.build/coverage/
          if-no-files-found: ignore
      # UpdateCheckIntegrationTests hits the real GitHub API, so this suite is the
      # repo's most plausible native flake source; the xUnit report records which
      # test was slow or failed on any given run.
      - name: Upload test timings
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-timings-swift-launcher
          path: packages/swift-launcher/.build/coverage/test-timings*
          if-no-files-found: ignore
      - name: Build overlay bootstrap (dist/ui/electron-overlay-entry.js)
        # `assemble-app.mjs` copies ONLY `dist/ui/electron-overlay-entry.js`
        # into the bundle so packaged `slicc-server --electron` loads the real
        # overlay instead of the inline fallback. The launcher no longer bundles
        # the rest of `dist/ui` (the UI loads from the hosted origin); the overlay
        # bootstrap is produced by the small `@ai-ecoverse/spoon` package (a fast,
        # webapp-free esbuild) and must exist before assembly or
        # `copyElectronOverlayEntry` fails fast.
        run: npm run build -w @ai-ecoverse/spoon
      - name: Install XcodeGen
        run: brew install xcodegen
      - name: Build
        run: npm run build -w @slicc/swift-launcher
      - name: Upload Sliccstart.app artifact (unsigned, for PR verification)
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: Sliccstart-${{ github.event.pull_request.number || github.run_id }}-unsigned
          path: packages/swift-launcher/build/Sliccstart.app
          if-no-files-found: error
      - name: Install Periphery
        run: brew install peripheryapp/periphery/periphery
      - name: Dead code detection (Periphery)
        run: |
          # Informational scan; surfaces unused Swift declarations without
          # blocking PRs while existing findings get cleaned up incrementally.
          cd packages/swift-launcher && periphery scan --disable-update-check || true

  ios-app:
    needs: [changes, lint]
    if: >-
      needs.lint.result == 'success' &&
      needs.changes.outputs.is-queue-leader == 'true' &&
      (needs.changes.outputs.ios-app == 'true' ||
      needs.changes.outputs.swift-traysession == 'true' ||
      needs.changes.outputs.swift-trayfollower == 'true' ||
      needs.changes.outputs.swift-traykit == 'true' ||
      needs.changes.outputs.swift-widgetkit == 'true' ||
      needs.changes.outputs.swift-config == 'true')
    # The iOS 26 deployment target requires the Xcode 26 SDK.
    runs-on: macos-26
    # Simulator boots can wedge on fresh runner images; fail fast instead of
    # holding the merge queue for the 6h default.
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      # swift-coverage-check.sh reads its floors from coverage-thresholds.json
      # via node, so pin Node (matches the other Swift jobs) instead of relying
      # on the runner's preinstalled version.
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
      - name: Install SwiftLint
        run: brew install swiftlint
      - name: Lint (SwiftLint)
        run: cd packages/ios-app && swiftlint lint --reporter github-actions-logging
      # swift-format ships with the toolchain; config is the root .swift-format.
      # Every target root in project.yml, not just the app: SwiftLint already
      # walks the whole package (it recurses from the package directory), so
      # naming only `SliccFollower` here left the framework and the three
      # app-extension targets — SliccTrayKit's ~1.3k lines of approval logic
      # included — formatted by nothing but the pre-commit hook.
      - name: Lint (swift-format)
        run: |
          cd packages/ios-app && swift format lint --strict --parallel --recursive \
            SliccFollower SliccTrayKit SliccFileProvider SliccShareExtension SliccWidgets Package.swift
      - name: Install XcodeGen
        run: brew install xcodegen
      - name: Generate Xcode project
        run: cd packages/ios-app && xcodegen generate
      - name: Resolve SPM dependencies
        run: |
          cd packages/ios-app
          xcodebuild -resolvePackageDependencies \
            -project SliccFollower.xcodeproj \
            -scheme SliccFollower
      # `-resolvePackageDependencies` rewrites Package.resolved in place and
      # then builds against whatever it wrote, so a floated pin used to reach
      # main without appearing in a single diff. `lint:swift-pins` cannot see
      # this: it only compares the direct pins named in project.yml and
      # Package.swift, and the ones that drift are transitive.
      - name: Check SwiftPM lockfile drift
        run: |
          node packages/dev-tools/tools/check-swift-resolved-drift.mjs \
            packages/ios-app/SliccFollower.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved \
            packages/ios-app/Package.resolved
      - name: Build (iOS Simulator)
        run: |
          set -o pipefail
          cd packages/ios-app
          xcodebuild build \
            -project SliccFollower.xcodeproj \
            -scheme SliccFollower \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO
      - name: Build (iOS device)
        run: |
          set -o pipefail
          cd packages/ios-app
          xcodebuild build \
            -project SliccFollower.xcodeproj \
            -scheme SliccFollower \
            -destination 'generic/platform=iOS' \
            CODE_SIGNING_ALLOWED=NO
      - name: Install Periphery
        run: brew install peripheryapp/periphery/periphery
      - name: Dead code detection (Periphery)
        run: |
          # Informational scan; surfaces unused Swift declarations without
          # blocking PRs while existing findings get cleaned up incrementally.
          # Unlike the SPM packages, an xcodegen project has no Package.swift for
          # Periphery to infer from, so the project/scheme/target are explicit.
          cd packages/ios-app && periphery scan \
            --project SliccFollower.xcodeproj \
            --schemes SliccFollower \
            --targets SliccFollower \
            --disable-update-check || true

  # Simulator test matrix: device × iOS runtime, all cells concurrent so the
  # slowest leg (not the sum of legs) bounds iOS wall-clock. The iPhone/26
  # cell is the canonical coverage-gated run; iPhone/27 repeats the unit suite
  # against the beta SDK without the coverage gate (line coverage shifts
  # between SDKs, and the floors track the stable one). iPad cells run the
  # regular-width browser regression. iOS 27 cells ride the `xcode-27` beta
  # runner image and are informational (continue-on-error) until 27 goes GA —
  # a beta-image breakage must not hold the merge queue.
  ios-app-tests:
    needs: [changes, lint]
    if: >-
      needs.lint.result == 'success' &&
      needs.changes.outputs.is-queue-leader == 'true' &&
      (needs.changes.outputs.ios-app == 'true' ||
      needs.changes.outputs.swift-traysession == 'true' ||
      needs.changes.outputs.swift-trayfollower == 'true' ||
      needs.changes.outputs.swift-traykit == 'true' ||
      needs.changes.outputs.swift-widgetkit == 'true' ||
      needs.changes.outputs.swift-config == 'true')
    strategy:
      fail-fast: false
      matrix:
        device: [iPhone, iPad]
        ios: ['26', '27']
    runs-on: ${{ matrix.ios == '27' && 'xcode-27' || 'macos-latest' }}
    continue-on-error: ${{ matrix.ios == '27' }}
    # Simulator boots can wedge on fresh runner images; fail fast instead of
    # holding the merge queue for the 6h default. 75 rather than 40 because the
    # GA cells run the whole UI bundle. Measured on this runner pool: ~11.4 min
    # to build, then ~29s per UI test, so 62 tests lands near 42 min, plus the
    # unit suite on the iPhone GA cell.
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      # swift-coverage-check.sh reads its floors from coverage-thresholds.json
      # via node, so pin Node (matches the other Swift jobs) instead of relying
      # on the runner's preinstalled version.
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
      - name: Install XcodeGen
        run: brew install xcodegen
      - name: Generate Xcode project
        run: cd packages/ios-app && xcodegen generate
      - name: Resolve SPM dependencies
        run: |
          cd packages/ios-app
          xcodebuild -resolvePackageDependencies \
            -project SliccFollower.xcodeproj \
            -scheme SliccFollower
      # Golden-fixture protocol corpus + unit tests. The shared coverage script
      # picks the simulator, runs `xcodebuild test` with coverage + parallel
      # testing + on-failure retries, and gates the result against the `ios-app`
      # floors in coverage-thresholds.json.
      - name: Test (iPhone unit suite, coverage threshold gate)
        if: matrix.device == 'iPhone' && matrix.ios == '26'
        run: |
          ./packages/dev-tools/tools/swift-coverage-check.sh \
            --xcodebuild SliccFollower packages/ios-app SliccFollower
      # Both non-coverage legs share ios-sim-test.sh, which reuses the
      # coverage script's simulator selection, SLICC_IOS_SIM_UDID override,
      # pre-boot, and runner-init retry — one canonical path, no drift.
      - name: Test (iPhone unit suite, no coverage gate)
        if: matrix.device == 'iPhone' && matrix.ios != '26'
        run: |
          ./packages/dev-tools/tools/ios-sim-test.sh \
            --device iPhone \
            --result-bundle .build/unit-suite.xcresult \
            --only-testing SliccFollowerTests
      # The WHOLE UI bundle, on both GA destinations, minus the reviewed
      # exclusions in packages/ios-app/ui-test-exclusions.json.
      #
      # This step replaced four hand-picked `-only-testing` classes. Under that
      # list 21 of the 25 UI classes ran nowhere but a developer's Mac, and two
      # tests had been failing on main for releases before anyone ran the suite
      # locally — the list was never wrong, it just could not grow itself.
      # Inverting it (run everything, subtract a registry) means a new class is
      # gated the day it lands, and leaving CI takes an edit to a reviewed file
      # with a written reason. `npm run lint:ios-ui-tests` keeps that registry
      # from going stale in the other direction.
      #
      # Both destinations, deliberately. The iPad is not redundant: it renders a
      # wider transcript with more rows per screen and carries the
      # readable-column cap, and #2048 was an iPad-only transcript regression.
      # It costs roughly 25 extra runner-minutes per GA cell; that is the price
      # of the suite being executed at all.
      - name: Test UI suite (whole bundle minus reviewed exclusions)
        if: matrix.ios == '26'
        run: |
          set -euo pipefail
          # Command substitution, not process substitution: `set -e` sees the
          # exit status here, so a broken registry fails the step instead of
          # silently degrading to "skip nothing" and running the known-red
          # tests.
          SKIP_SPECS="$(node packages/dev-tools/tools/ios-ui-test-exclusions.mjs --print-skip-args)"
          SKIP_ARGS=()
          while IFS= read -r spec; do
            # `if`, not `[ … ] && …`: under `set -e` a false trailing test in a
            # loop body aborts the step, and an empty registry produces one
            # empty line.
            if [ -n "$spec" ]; then SKIP_ARGS+=(--skip-testing "$spec"); fi
          done <<< "$SKIP_SPECS"
          ./packages/dev-tools/tools/ios-sim-test.sh \
            --device ${{ matrix.device }} \
            --result-bundle .build/ui-suite.xcresult \
            --only-testing SliccFollowerUITests \
            ${SKIP_ARGS[@]+"${SKIP_ARGS[@]}"}
      # The beta-SDK cells keep a curated slice, not the whole bundle. They are
      # `continue-on-error` canaries for SDK breakage, not the coverage surface,
      # and paying the full bundle twice more to learn that Xcode 27 moved a hit
      # region is not worth the queue time. What is worth carrying is the
      # layout- and scroll-sensitive set, because that is what an SDK bump
      # actually breaks: #2072 asserts a MEASURED drift across a keyboard
      # transition (a rendering assertion sails past a reintroduction), and the
      # two iPad classes cover the regular-width column and full-screen tab
      # layout that only a regular-width destination can see.
      - name: Test transcript scroll anchoring (#2072, beta SDK)
        if: matrix.ios != '26'
        run: |
          ./packages/dev-tools/tools/ios-sim-test.sh \
            --device ${{ matrix.device }} \
            --result-bundle .build/transcript-anchor-ui.xcresult \
            --only-testing SliccFollowerUITests/TranscriptComposerGrowthUITests
      # The transcript's short actions are the one feature here that CANNOT be
      # proven below the view layer: the unit suite fixes which spans become
      # actionable, but whether a tap and a long press actually reach them is a
      # property of the running text engine. Prose paints through a UITextView
      # precisely because SwiftUI `Text` has no per-span long press, and nothing
      # short of a real gesture can tell whether that wiring survived.
      #
      # iPhone only: compact width is where a transcript is read on a phone, and
      # the menus are identical at regular width. Beta cells only: the GA cells
      # already run this class inside the whole bundle above, and a live text
      # engine is exactly what an SDK bump moves, so the canary keeps it.
      - name: Test transcript short actions (iPhone, beta SDK)
        if: matrix.device == 'iPhone' && matrix.ios != '26'
        run: |
          ./packages/dev-tools/tools/ios-sim-test.sh \
            --device iPhone \
            --result-bundle .build/transcript-short-actions-ui.xcresult \
            --only-testing SliccFollowerUITests/TranscriptShortActionsUITests
      - name: Test regular-width browser (iPad, beta SDK)
        if: matrix.device == 'iPad' && matrix.ios != '26'
        run: |
          ./packages/dev-tools/tools/ios-sim-test.sh \
            --device iPad \
            --result-bundle .build/regular-width-ui.xcresult \
            --only-testing SliccFollowerUITests/RemoteTabUITests/testRegularWidthBrowsingEntersAndExitsFullScreen
      - name: Test regular-width transcript column (iPad, beta SDK)
        if: matrix.device == 'iPad' && matrix.ios != '26'
        run: |
          ./packages/dev-tools/tools/ios-sim-test.sh \
            --device iPad \
            --result-bundle .build/transcript-column-ui.xcresult \
            --only-testing SliccFollowerUITests/TranscriptColumnUITests
      - name: Upload coverage report
        if: always() && matrix.device == 'iPhone' && matrix.ios == '26'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-ios-app
          path: packages/ios-app/.build/coverage/lcov.info
          if-no-files-found: ignore
      # Per-test timings live in the result bundle; upload it so a slow or flaky
      # test can be diagnosed without re-running the simulator locally.
      - name: Upload test timings (xcresult)
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: test-timings-ios-app-${{ matrix.ios }}-${{ matrix.device }}
          # Glob, not a hand-kept list: the earlier enumeration named three of
          # the bundles and silently dropped every leg added after it, so the
          # UI runs whose flakes actually need diagnosing were the ones with no
          # artifact.
          #
          # `include-hidden-files` is load-bearing. An `.xcresult` is a bundle
          # whose contents include dot-prefixed entries, and upload-artifact
          # skips those by default — the first version of this step matched the
          # bundles and still uploaded nothing ("No files were found"), which is
          # how a CI-only UI failure ended up with no screenshot or accessibility
          # hierarchy to read. The sibling Swift jobs never hit it because they
          # upload plain files.
          include-hidden-files: true
          path: |
            packages/ios-app/.build/coverage/ios-app.xcresult
            packages/ios-app/.build/*.xcresult
          if-no-files-found: ignore

  global-install:
    needs: changes
    if: >-
      needs.changes.outputs.cloud-core == 'true' ||
      needs.changes.outputs.node-server == 'true' ||
      needs.changes.outputs.shared == 'true' ||
      needs.changes.outputs.root-config == 'true'
    runs-on: ubuntu-latest
    # Backstop, not a budget: a hung `npm ci` (see .github/actions/npm-ci)
    # must not sit on a runner until the 6-hour default fires.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - uses: ./.github/actions/npm-ci
      - name: Build @slicc/shared-ts
        run: npm run build -w @slicc/shared-ts
      - name: Build node-server
        run: npm run build -w @slicc/node-server
      - name: Pack
        run: echo "tarball=$(npm pack --quiet | tail -1)" >> "$GITHUB_OUTPUT"
        id: pack
      # `npm install -g <tarball>` and `npx sliccy@file:…` resolve the packed
      # tarball's dependencies FRESH from the registry — the repo lockfile does
      # not apply to either — so this is the one job whose result depends on
      # registry state at the moment it runs. That is deliberate (it is what a
      # real `npm i -g sliccy` does), but it means transient registry flux fails
      # a job that has nothing to do with the diff under test.
      #
      # Seen 2026-08-20: a newly published `@peculiar/*` release pinned
      # `@peculiar/asn1-schema@^2.9.4` before that version had replicated to the
      # runner's registry mirror, and the install died with
      # `ETARGET No matching version found`. The same commit passed this job 15
      # minutes earlier and again 15 minutes later. npm's own fetch-retries do
      # not cover it — the packument fetch SUCCEEDS, it just does not list the
      # version yet — so nothing retried and a green PR (#2216) was dequeued from
      # the merge queue, where a run gets no second chance.
      #
      # `prefer-online` carries as much weight as the loop: without it a retry
      # can re-read the same stale packument from the runner's metadata cache and
      # fail identically. A dependency that genuinely does not resolve still
      # fails, just after ~45s of trying.
      - name: Test global install
        env:
          npm_config_prefer_online: 'true'
        run: |
          set -o pipefail
          attempts=3
          for i in $(seq 1 "$attempts"); do
            echo "::group::npm install -g (attempt $i/$attempts)"
            status=0
            npm install -g "./${{ steps.pack.outputs.tarball }}" || status=$?
            echo "::endgroup::"
            if [ "$status" -eq 0 ]; then
              break
            fi
            if [ "$i" -ge "$attempts" ]; then
              echo "global install failed (exit $status) after $attempts attempts" >&2
              exit "$status"
            fi
            delay=$((i * 15))
            echo "global install failed (exit $status) on attempt $i/$attempts; retrying in ${delay}s" >&2
            sleep "$delay"
          done
          slicc --version
      - name: Test npx
        env:
          npm_config_prefer_online: 'true'
        run: |
          set -o pipefail
          attempts=3
          for i in $(seq 1 "$attempts"); do
            echo "::group::npx (attempt $i/$attempts)"
            status=0
            npx --yes "sliccy@file:./${{ steps.pack.outputs.tarball }}" --version || status=$?
            echo "::endgroup::"
            if [ "$status" -eq 0 ]; then
              exit 0
            fi
            if [ "$i" -ge "$attempts" ]; then
              echo "npx failed (exit $status) after $attempts attempts" >&2
              exit "$status"
            fi
            delay=$((i * 15))
            echo "npx failed (exit $status) on attempt $i/$attempts; retrying in ${delay}s" >&2
            sleep "$delay"
          done

  # Wait for any in-progress release before allowing merge queue to land.
  # Only runs on merge_group events; skipped (= passing) for PRs.
  #
  # The timeout must clear a whole release, because the `ci` aggregate below
  # treats `cancelled` exactly like `failure` — so a gate killed by its own
  # timeout dequeues every PR in the batch for a reason that has nothing to do
  # with their contents. Over the 330 releases in the month to 2026-08-24:
  # p50 20m, p90 28m, p95 31m, p99 44m, max 60m. The former 35m sat between p95
  # and p99 and fired on 9 of them; #2380 was dequeued after entering the queue
  # two minutes into a 60-minute release. 90m clears the observed max with 50%
  # headroom. Raise it if a release ever legitimately exceeds it — do NOT trim
  # it back toward the median, and note that idle wait costs one sleeping runner
  # while a dequeue costs a full batch re-run.
  release-gate:
    if: github.event_name == 'merge_group'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      actions: read
    steps:
      - name: Wait for active release
        env:
          GH_TOKEN: ${{ github.token }}
          # Below `timeout-minutes` so the loop reports WHY it gave up. A job
          # killed by the runner timeout can emit nothing, and the aggregate
          # gate then prints only its generic "failed or were cancelled" line.
          DEADLINE_MINUTES: '85'
        run: |
          deadline=$(( $(date +%s) + DEADLINE_MINUTES * 60 ))
          while true; do
            ACTIVE=$(gh api "repos/${{ github.repository }}/actions/workflows/release.yml/runs?status=in_progress&per_page=1" --jq '.total_count')
            QUEUED=$(gh api "repos/${{ github.repository }}/actions/workflows/release.yml/runs?status=queued&per_page=1" --jq '.total_count')
            if [ "$ACTIVE" = "0" ] && [ "$QUEUED" = "0" ]; then
              echo "No release in progress, proceeding"
              break
            fi
            if [ "$(date +%s)" -ge "$deadline" ]; then
              echo "::error::Release still running (active=$ACTIVE queued=$QUEUED) after ${DEADLINE_MINUTES}m; giving up so this batch does not merge mid-release. Re-enqueue once the release completes."
              exit 1
            fi
            echo "Release running (active=$ACTIVE queued=$QUEUED), waiting 30s..."
            sleep 30
          done

  # Bundle-size budgets for the two Vite-bundled browser apps. Before this
  # job the only shipped-size gate in the repo was the worker's
  # `wrangler deploy --dry-run` (Cloudflare's 25 MiB per-file asset cap), so
  # webapp and extension growth was invisible until someone noticed a slow
  # cold load. Budgets live next to the code they guard, in each package's
  # `size-limit` block, and are measured on raw (non-brotli) bytes to match
  # the asset-cap framing of the worker gate.
  #
  # The webapp additionally gates first-load performance:
  # check-first-load-size.mjs measures the EAGER import closures of the page
  # and kernel-worker entries (what a cold-cache boot actually downloads).
  # That check is RELATIVE — it builds this change's merge-base in a
  # throwaway worktree and fails only when the change itself grows a graph by
  # more than `maxDeltaKb`. It used to be an absolute number, which failed:
  # the budget was set to main's exact measurement, main measures ~1 kB
  # larger here than on the macOS machines developers verify on, and so every
  # webapp PR inherited a red gate it had not caused (#2422, #2436, #2438 all
  # measured exactly main's number and all patched the same line). The
  # absolute ceilings in packages/webapp/first-load-budget.json remain as the
  # backstop against many small under-threshold changes creeping upward.
  #
  # `dev-tools` is in the filter list because the webapp inlines
  # `packages/dev-tools/providers.build.json` at build time to decide which
  # providers get bundled, so editing it alone moves the shipped size.
  bundle-size:
    needs: changes
    if: >-
      needs.changes.outputs.webapp == 'true' ||
      needs.changes.outputs.extension == 'true' ||
      needs.changes.outputs.vfs-root == 'true' ||
      needs.changes.outputs.assets == 'true' ||
      needs.changes.outputs.shared == 'true' ||
      needs.changes.outputs.spoon == 'true' ||
      needs.changes.outputs.webcomponents == 'true' ||
      needs.changes.outputs.cherry == 'true' ||
      needs.changes.outputs.dev-tools == 'true' ||
      needs.changes.outputs.root-config == 'true'
    runs-on: ubuntu-latest
    # Backstop, not a budget: a hung `npm ci` (see .github/actions/npm-ci)
    # must not sit on a runner until the 6-hour default fires.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          # The first-load check builds this change's merge-base to measure
          # the delta, so it needs history and an `origin/main` ref.
          fetch-depth: 0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - uses: ./.github/actions/npm-ci
      - name: Build webapp
        run: npm run build -w @slicc/webapp
        env:
          NODE_OPTIONS: '--max-old-space-size=6144'
      - name: Build extension
        run: npm run build -w @slicc/chrome-extension
      - name: Check bundle-size budgets
        run: npm run bundle-size

  # Summary job — the single required status check for branch protection
  ci:
    if: always()
    needs:
      [
        changes,
        lint,
        linear-history,
        typecheck,
        node-matrix-tests,
        webapp,
        e2e,
        node-server,
        chrome-extension,
        cloudflare-worker,
        cloud-core,
        shared,
        webcomponents,
        spoon,
        bundle-size,
        global-install,
        swift-launcher,
        swift-server,
        swift-optel,
        swift-traysession,
        swift-trayfollower,
        swift-traykit,
        swift-widgetkit,
        ios-app,
        ios-app-tests,
        go-optel,
        slicc-cli,
        release-gate,
      ]
    runs-on: ubuntu-latest
    steps:
      - name: Check job results
        run: |
          if [[ "${{ contains(needs.*.result, 'failure') }}" == "true" || "${{ contains(needs.*.result, 'cancelled') }}" == "true" ]]; then
            echo "::error::One or more jobs failed or were cancelled"
            exit 1
          fi
