node_modules/
dist/
*.tsbuildinfo
.DS_STORE
.env
.env.local
.env.*.local
/workspace/
coverage/
.worktrees/
worktrees/
.yolo/
.wrangler/
.qa/
artifacts/
providers.json
/providers/
!/providers/.gitkeep
!/providers/adobe.ts
!/providers/adobe-config.json
.superpowers/
test-results/
test-timing/
playwright-report/
.playwright-cli/
packages/swift-launcher/.node-cache/
/tasks/
.claude/settings.*
.claude/worktrees/
packages/cloudflare-worker/.dev.vars

# Build-generated by packages/cherry/scripts/build-preview-bootstrap.mjs (runs in
# postinstall via the cherry build). Never commit it: it's a ~232 KiB minified
# IIFE that drifts by dep/toolchain version, so a committed copy goes stale and
# any full build rewrites it. Regenerated on every `npm ci`/`npm install`.
packages/cloudflare-worker/src/preview-bridge-assets.ts

# RUM error-triage runtime artifact
rum-error-candidates.json

# Scratch hand-off files the scheduled agentic workflows write to the repo root
# for their Claude phases to read. The authoring phases run `git` with write
# access to a branch, so an un-ignored scratch file is one `git add -A` away
# from landing in a pull request.
rum-fix-ready-issues.json
backlog-candidates.json
backlog-ready-issues.json
