{
  "name": "@slicc/chrome-extension",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build --config vite.config.ts",
    "build:dev": "SLICC_EXT_DEV=1 vite build --config vite.config.ts --mode development",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p ../../tsconfig.json"
  },
  "dependencies": {
    "@imagemagick/magick-wasm": "^0.0.39",
    "@mariozechner/pi-ai": "^0.63.0",
    "@mariozechner/pi-coding-agent": "^0.63.0"
  }
}
