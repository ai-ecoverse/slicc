{
  "name": "@slicc/node-server",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --input-type=module -e \"import { rmSync } from 'node:fs'; rmSync('../../dist/node-server', { recursive: true, force: true });\" && tsc -p ../../tsconfig.cli.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p ../../tsconfig.cli.json"
  }
}