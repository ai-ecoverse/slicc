{
  "name": "@slicc/local-models-eval",
  "private": true,
  "type": "module",
  "description": "Optional eval suite that drives a SLICC-shaped agent loop against a running SwiftLM (or any OpenAI-compatible) endpoint. Built directly on @mariozechner/pi-agent-core + pi-ai so the call shape and tool format match production exactly.",
  "scripts": {
    "eval": "tsx src/index.ts",
    "eval:smoke": "tsx src/index.ts --scenario parallel_math",
    "eval:list": "tsx src/index.ts --list",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@mariozechner/pi-agent-core": "^0.72.0",
    "@mariozechner/pi-ai": "^0.72.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.4",
    "typescript": "^5.7.0"
  }
}
