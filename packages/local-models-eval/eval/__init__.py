"""Optional end-to-end eval suite for the local-models stack.

Public surface is intentionally small — `agent_loop.run` plus the
`scenarios.SCENARIOS` registry. Everything else is implementation
detail and may move around between commits.
"""
