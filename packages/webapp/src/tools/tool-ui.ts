/**
 * Compatibility re-export for the tool UI registry, which lives in the lower
 * shell layer so shell commands can use it without an upward layer back-edge.
 */
export * from '../shell/tool-ui.js';
