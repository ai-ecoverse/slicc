/** Foundational webapp aliases for the shared runtime-environment primitives. */
export {
  canConnectToChromeRuntime as hasChromeRuntimeConnect,
  isChromeExtensionRealm as isExtensionRealm,
} from '@slicc/shared-ts';
