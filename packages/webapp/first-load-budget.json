{
  "//": "First-load (cold-cache) eager-import-closure limits for the webapp, checked by packages/dev-tools/tools/check-first-load-size.mjs after a webapp build. pageEagerKb = static import graph of index.html; workerEagerKb = static import graph of the kernel-worker entry.",
  "//delta": "maxDeltaKb is the PRIMARY gate: a PR may not grow either eager graph by more than this much versus its own merge-base, which the checker builds and measures on the same machine in the same run. Relative, so it fails only PRs that actually regress and never drifts with the platform. 4 kB was calibrated against real traffic, not guessed: eight in-flight PRs measured 0.00-1.54 kB, while a deliberately staged regression (a static `unpdf` import in the boot-critical supplemental-commands index) measured +7.0 kB. 4 kB sits in that gap.",
  "//ceiling": "The ceilings are the SECONDARY gate and the human-owned line in the sand. A delta gate alone lets many small under-threshold PRs creep the graph upward forever; these bound the total. They are NOT ratchets to nudge when a build goes red — raising one is a deliberate decision about cold-boot payload and needs a reason in the PR body. Tighten them freely as the graphs shrink.",
  "//delta-raised": "Raised 4 -> 5 for the biscotti guest-seat subsystem (PR #2535, 2026-08-28). NOT a hoist: the instruction markdown, the approver agent, and the whole directed-approval flow are behind first-use imports and verifiably absent from the worker bundle; the +4.2 kB that remains is code on the paths it gates (turn-gate state, the adaptTool hook, approval-router scoping). Every lazy-loading lever was taken first, and removing the boot wiring entirely bought more than byte-shaving did. This is a ceiling-style deliberate decision, not a ratchet to nudge: the next PR that comes in over 4 kB should be treated as a regression and investigated, not waved through, and this should be put back to 4 once the subsystem has landed.",
  "//delta-raised-2417": "Raised 5 -> 14 for the CDP bridge session-model fix (issue #2417). NOT a hoist: measured per module (minified, vs merge-base), the ~+12.5 kB is ~+8.4 kB cdp/browser-api.ts (per-tab session registry with detach/LRU and in-flight pinning, stale-session self-heal gated to the first round trip, owner-token bridge lock with page waits released, session-scoped navigate, transport-scoped session clearing), +1.6 kB kernel/cdp-page-forwarder.ts (relays cdp-reset/cdp-ready, lazy re-dial that also waits through a connecting client, drops commands that crossed a reset), +0.8 kB kernel/cdp-bridge.ts (state notifications for bridge transports), +0.7 kB cdp/cdp-client.ts (4002 upstream-reset, single specific close notification), +0.7 kB navigation-watcher.ts (own-session gating, restart after reset), +1.0 kB playwright session-rebind + handlers (re-arm captures on a replaced session), -0.4 kB cdp-worker-proxy.ts (page-only code moved out). All of it is code on the boot-critical bridge paths it gates and nothing lazy-loadable remains. Same rule as above: treat the next PR over 5 kB as a regression, and put this back to 5 once #2417's follow-up (session-explicit handlers) lands. DONE: the follow-up landed (session-explicit `TabHandle`), and this is back to 5.",
  "//ceiling-raised-3030": "workerEagerCeilingKb 4288 -> 4290 for the leading cone's cross-cone scoop reach (PR #3030). NOT a hoist and not a nudge: the delta gate measured +2.3 kB vs merge-base, all of it code and tool-schema text inside scoops/scoop-management-tools.ts and work-unit/{policy,record}.ts — modules the kernel worker already loads eagerly to build a unit's tool set, so no lazy lever exists and nothing new entered the graph (chunk count unchanged at 91). ~0.7 kB was shaved back first by tightening the tool descriptions and folding the two unresolved-name error builders and the two relation formatters into one each; what remains is the ownership classification, the cross_cone gate and the messages that make a refusal legible, which is the feature. Base was 4286.7 kB, leaving only 1.7 kB under the old ceiling — the raise buys ~1.4 kB of headroom, not room to creep. Tighten it again as the worker graph shrinks.",
  "//ceiling-raised-3035": "workerEagerCeilingKb 4290 -> 4292 for trackError details coercion (issue #3035). NOT a hoist: the delta gate measured +1.3 kB vs merge-base, all of it core/error-text.ts pulled into the kernel-worker graph through kernel/telemetry.ts (trackError is sync and already eager). No new chunk (still 91). Measured worker 4291 kB, 1 kB over the old ceiling; the raise buys ~1 kB of headroom, not room to creep. Tighten it again as the worker graph shrinks.",
  "//ceiling-raised-2857": "workerEagerCeilingKb 4292 -> 4294 for preview Range from a partial VFS read (issue #2857). NOT a hoist: the delta gate measured +0.8 kB vs merge-base, all of it VirtualFS.readFileRange (lazy File.slice) plus the vfs-read-file start/end path on VfsRpcHost / RemoteVfsClient — modules the kernel worker already loads eagerly for every VFS RPC. No new chunk. Measured worker 4293 kB, 1 kB over the old ceiling; the raise buys ~1 kB of headroom, not room to creep. Tighten it again as the worker graph shrinks.",
  "//ceiling-raised-3005": "workerEagerCeilingKb 4294 -> 4296 for the gelatiere resident advisor (PR #3005; originally 4292 -> 4294, stacked +2 on top of #2857's raise at rebase). NOT a hoist: the delta gate measured +1.4 kB vs merge-base, and every lazy lever was taken first — the unit and store modules (with the bundled GELATIERE.md) load via dynamic import from a fire-and-forget host hook, the `gelatiere` command shim is 763 source bytes with lazy verb dispatch, and base/gelatiere-constants.ts exists precisely so scoop-context.ts can classify the unit without hoisting the store. What remains eager is the sync unit classification (isGelatiereUnit in the scoop lifecycle), the command registration, and the host wiring, which is the feature. Re-measured at the Track C tip (memory command, dreaming, wiki, memory-health, all lazy): the delta gate reads +3.7 kB vs merge-base; macOS measures the worker at exactly 4296 kB and Linux CI measures ~1 kB above macOS (main: 4293 kB on Linux), which rounds to 4297 and would trip 4296, so the ceiling is 4298 (originally 4292 -> 4294 -> 4296 -> 4298 across rebases). The raise buys ~1 kB of headroom on CI, not room to creep. Tighten it again as the worker graph shrinks.",
  "maxDeltaKb": 5,
  "pageEagerCeilingKb": 768,
  "//ceiling-raised-3053": "Approved for the durable tray/webhook lifecycle work in PR #3053: measured worker eager graph growth versus merge-base with page unchanged. Raise 4298 -> 4299 for this wiring and narrow headroom (stacked on #3005's 4298 ceiling); keep the 5 kB per-PR delta gate and page ceiling unchanged.",
  "workerEagerCeilingKb": 4299
}
